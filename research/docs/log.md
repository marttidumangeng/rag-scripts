---
type: log
title: Research Docs Changelog
status: published
version: 1.0
owner: AI
last_updated: 2026-07-31
tags:
  - research
---

# Research Docs Changelog

## 2026-07-31

- **Discovery + Enrichment no longer leave robots with "No country" or "No category."** Root cause was three separate holes, all measured on prod that day (1596 pending: 351 no country, 317 no category, 295 both):
  - *Nothing ever wrote a category.* The Gemini classifier produces `sub_category`, which resolves to `RobotSubCategory` ("Applications") — a different model from the `Category` M2M that `quality.missing_category` counts. New [`robot_categories.py`](../robot_categories.py) derives `category_slugs` from movement types / sub-category / uses / industries / name keywords against a fixed vocabulary of slugs that already exist on prod; wired into `robot_auto_research`, `discover_robots` and `manufacturer_gap_discovery`.
  - *`missing_manufacturer_country` was listed as UNFIXABLE* despite being error-severity and deterministic. New [`company_country_resolve.py`](../company_country_resolve.py) (ccTLD → site address → serper → Gemini, fail-closed) plus `remedy_missing_manufacturer_country`, which writes the **Company** as well as the robot so siblings are fixed for free. 232 of 806 companies had no country.
  - *`robot_gaps()` reported neither*, and `overnight_queue_enrich` filters its work queue on it — so a robot whose only problems were these two was invisible to every remediation pass.
- Also: `missing_category` and `missing_taxonomy` shared the `refresh_taxonomy` action, so a NO_OP under one blocked the other (category now uses `refresh_category`). `resolve_pending_company_websites.py` → **`resolve_pending_company_gaps.py`**, now resolving website *and* country in one queue scan. `api_client.get_subcategories()` raised `AttributeError` on every call (bare-list response). Bulk import's `_get_categories` now RESOLVES before creating and refuses sentence fragments — the unguarded `get_or_create` is why prod holds 191 categories with 135 unused. Tests: `test_robot_categories.py`, `test_company_country_resolve.py`, `test_country_category_coverage.py`, `test_bulk_import_categories.py`.

## 2026-07-30

- Gap-queue: rejected MIC SEO shells for Dongguan Rainbow **1496** (19) and Guangzhou Aobo **1482** (14, dupe of 1384). Enriched Guanhong **1419** (18) via `fix_guanhong_1419_robots.py` + hero repair; 5 All-In-One imageless (LinkedIn logo placeholder). Marked done 1496/1482/1419.
- Created **Israel Aerospace Industries (IAI)** company **1648**; reassigned Palladyne IAI SKUs Mini HARPY/HARPY/HAROP (#5675/#5672/#5671) via `fix_iai_reassign_from_palladyne.py`; enriched from `iai.co.il`; CDN 3/3; marked done 1621 + 1648.
- Symbotic (1623) curated enrichment via `fix_symbotic_1623_robots.py`: SymMicro #5686 — system-render hero, features/purpose/taxonomy, US, Available(11), CDN OK. Marked done 1623.
- Coco Robotics (1619) curated enrichment via `fix_coco_1619_robots.py`: renamed Delivery→Coco 2 #5657, URL `/coco2`, speed 21 km/h, Framer studio hero, CDN OK. Marked done 1619.
- Palladyne AI (1621) curated enrichment via `fix_palladyne_1621_robots.py`: 7 own products enriched (3 imageless IMAGE TO-DO); 3 IAI partner SKUs attribution-hold only — **not marked done** pending reject/reassign decision.

## 2026-07-29

- Ecovacs Robotics (32) full enrichment via `fix_ecovacs_32_robots.py`: **36 keepers / 7 rejects** / 1 IMAGE TO-DO (#2475). Series `family_key`s; restored feat=0 BLACK twins; purpose≠description; Available(11); CDN **38/38** distinct heroes. Left `pending_review`. Marked done 32. Report: `staging/reports/ecovacs-32-enrichment.md`.
- Doosan Robotics (193) curated enrichment via `fix_doosan_193_robots.py`: **14 keepers / 14 rejects** (same-SKU PDP dupes). Heroes `{sku}-slide01.jpg` md5-unique; catalog PDF weights; family `doosan-robotics:{e|a|m|h|p}-series`; stripped name prefix; cleared Humanoid/AMR/Drone tags; Available(11); KR; CDN **30/30**. Left `pending_review`. Marked done 193. Report: `staging/reports/doosan-193-enrichment.md`.
- Avidbots (305) curated enrichment via `fix_avidbots_305_robots.py`: keepers **Meet Kas #3847**, **Neo 2 #3845**, **Neo 2W #2679**; rejected Neo 2W dupe **#3846→2679**. Cleared fake payloads (Kas downforce 36.8; FAQ GVW 581.5); OEM tech-sheet dims/speed/weight; CA country; Cleaning/AMR tags; CDN **6/6**. Left `pending_review`. Marked done 305. Report: `staging/reports/avidbots-305-enrichment.md`.
- EP Equipment (1274) curated enrichment via `fix_ep_1274_robots.py`: **20/20** pending_review (0 rejects). Live PDPs for 10 keepers; brochure/overview for 10 (IMAGE TO-DO). JXO→JX0; RPL251/301 family-scope; EPT25 payload 2500; cleared multi-option/tow payloads; distinct EPT attr_11 heroes; CDN **10/10** keepers (10 intentional imageless). `family_key=ep-equipment:{series}`. Marked done 1274. Report: `staging/reports/ep-1274-enrichment.md`.
- DJI (27) curated enrichment via `fix_dji_27_robots.py`: **16 keepers** / **7 rejects** (series shells 413–417, Agras 108, FlyCart 418). EN OEM PDPs + specs; CMS heroes md5-unique; cleared fabricated camera payloads; fixed family keys; zh translation-sync force; copy-media 16/16; CDN **23/23**. Left `pending_review`. Marked done 27. Report: `staging/reports/dji-27-enrichment.md`.
- LimX Dynamics (68) curated enrichment via `fix_limx_68_robots.py`: **7 keepers** (Luna/Oli/TRON1/TRON2 + imageless W1/CL-1/P1), **3 rejects** (4856 TRON1 dupe→158, 4855 CL-3→670 Oli, 2170 non_robot shell). OEM EN PDPs + `/spec` tables; cleared fabricated payload_kg=10 (except OEM TRON1 load ≤10 kg); heroes Luna/Oli/TRON2 CDN 200; W1/CL-1/P1 IMAGE TO-DO. family_key `limx-dynamics:{series}`; slug stays `limx-dynamics-2`. Marked done 68. Report: `staging/reports/limx-68-enrichment.md`.
- Wesar Intelligence (1476) curated enrichment via `fix_wesar_1476_robots.py`: **33 keepers / 17 rejects**. Cleared fabricated `payload_kg=1000`; OEM EN PDP tables; `family_key=wesar:{cu|tp5|tp1|f1|f3|f4|q|qf}`; CN + Available(11) (F3-1000 discontinued). WP slug≠model remaps confirmed via titles. Fail-closed 7 imageless (shared F3/F4/TP5 bytes + CU1-1500C). CDN keeper heroes hash-unique; marked done 1476. Report: `staging/reports/wesar-1476-enrichment.md`.
- AgXeed (264) curated enrichment via `fix_agxeed_264_robots.py`: **4 pending_review enriched** (W4/W3/T2.5/T2.7 SERIES), **1 rejected** (4031 W4.2 → keep 1537). OEM Specsheet PDFs for typed weight/speed/dims; cleared false payload 500; family `agxeed:{w4-2|w3-2|t2-5|t2-7}`; NL; Available(11); CDN **4/4** distinct. Report: `staging/reports/agxeed-264-enrichment.md`.
- Aobo Robot (1384) QA fix on 15 To-Review robots: hero swaps (XiaoAn r2, DaBen r2), strip generic/sibling videos (ORM), Reception-vs-delivery aligned to datasheets, KaKa Welcome imageless, verification flags cleared. CDN **42/43** (1993 imageless). Script: `fix_aobo_1384_qa.py`. Report: `staging/reports/aobo-1384-qa-fix.md`.
- Aobo Robot (1384) content-queue enrich: 25 keepers updated (`created_count=0`), 18 URL-dupes rejected via API fallback, copy-media 24/24, CDN verify 43/43. Script: `fix_aobo_1384_robots.py`. Report: `staging/reports/aobo-1384-enrichment.md`. Hold: 1993 KaKa Welcome imageless (shared OEM hero with 1997; stale banner photo needs ORM delete).

## 2026-07-28

- Elephant Robotics (216) curated enrichment via `fix_elephant_216_robots.py`: **15 pending_review enriched**, **1 rejected** (450 MarsCat dupe → keep 2483). Fixed uniform `payload_kg=3.0`, shared CDN hero hash on 2482–2493, broken doubled URLs, empty `family_key`, C3/catbot naming. CDN **16/16** OK, **0** pending hero collisions. Marked done 216. Report: [overnight-216-elephant.md](../staging/reports/overnight-216-elephant.md).
- Drone Delivery Canada (1509) curated enrichment via `fix_ddc_1509_robots.py`: **4/4 pending_review** (Sparrow/Canary/Robin XL/Condor) — OEM heroes, family_*, Available/Announced, typed payload/speed/MTOW, CA country + website `dronedeliverycanada.com`. CDN **4/4** distinct. Left `pending_review`. Report: `staging/reports/overnight-1509-ddc.md`.
- Mujin (810) completeness pass via `fix_mujin_810_robots.py`: renamed JP display names (3763/3753), re-PATCHed Available(11)+JP+taxonomy, documented AGV multi-SKU table + cell work limits in features (not arm typed columns), kept 3756 as mixed-load layout variant of 3757, soft-held 3755 RCP / 3758 Pallet Changer. CDN **10/10** distinct. Approve allowlist: **3753, 3754, 3756, 3757, 3759, 3760, 3762, 3763**. Soft leftover: 3758 TruckBot video 6941 needs staff soft-delete. Marked done 810. Report: [overnight-810-mujin.md](../staging/reports/overnight-810-mujin.md).
- Rainbow Robotics (228) full enrichment via `fix_rainbow_228_robots.py`: **14 pending_review enriched**, **2 rejected** (4240 duplicate RB-Y1 → keep 4861; 4859 RB16 alias → keep 2211). Fixed shared cobot CDN collision, restored OEM payload/reach/weight/repeatability, corrected RBM-S100b specs, added RST heroes from rainbowastro + OEM. Families `rainbow-robotics:{rb|rb-y1|rbq|rbm-s100|rst}`; KR; Available/Announced. CDN **14/14** distinct. Left `pending_review`.
- Hanwha Robotics (227) curated enrichment via `fix_hanwha_227_robots.py`: **20 pending_review enriched**, **0 rejected**, **0 imageless**; corrected Hanwha PDP typed `payload_kg` misparse (label-based override), extracted cobot dims/DOF, set family keys `hanwha-robotics:{hcr|amr|agv}`, availability `Available(11)`, OEM-purpose one-per-line, TagCatalog exact tags, and kept only model-token video titles. Copy-media applied for 18/20 robots (transient 502 for Roll/Reel) then CDN verify **20/20** distinct HTTP200. Marked done 227. Report: `staging/reports/hanwha-227-enrichment.md`.

## 2026-07-22

- Foshan Tefude Automation (1404) curated full enrichment via `fix_tefude_1404_robots.py`: company website → `https://www.tefude.com/` + China; remapped SEO shells to citeable OEM SKUs — **TFD-RD4-600A/800A/1200A/1600A** (2503/1972/2504/1973), **TFD-RP4-2200-30/50** (1975/1974), **4-Axis Industrial Joint Parallel Robot** (1978); rejected **1970/1971/1976/1977** (category/packing-cell/carton-sealer/SEO dupe). Replaced shared site-banner hero (`728e8af0…`) then **RD4 follow-up** (`fix_tefude_1404_rd4_heroes.py`) swapped text-heavy packing-line composites for robot-dominant DETAIL DISPLAY crops + TEFUDE cell shot; RP4/joint keep TEFUDE factory photos. Families `foshan-tefude-automation:{tfd-rd4|tfd-rp4-2200|joint-parallel}`; Available(11); typed RD4/RP4 column specs. Final **7 pending_review / 4 rejected / 0 published**. Approve allowlist: **2503, 1972, 2504, 1973, 1975, 1974, 1978**. Left `pending_review` (no auto-publish).
- Stakeholder approved Hitbot (976); API verified **7 published / 0 pending**. Moved to Cleared. Next enrichment: Foshan Tefude Automation (1404).
- Hitbot Technology (976) hero fix via `fix_hitbot_976_heroes.py`: replaced nav line-icons + dimensional `drawing.jpg` + wrong-brand HIWIN `S922.png` with official HITBOT-branded product renders for all **7** robots (6 pending + published S922). CDN verify **7/7** distinct hashes. Avoided HIRATA/HIWIN-contaminated 2442 assets. Dead PDPs for 1522/1632/2140/6140 remapped to `https://en.hitbot.cc/z-arm.html`.
- Stäubli Robotics (1475) curated via `fix_staubli_1475_robots.py`: **19 keepers / 26 rejects**. Keepers: TS2-40/60/80/100, TX2-40/60/90/140/160/200/200L, TX2-60L MedX Ready, TX2-60L/90L/90XL/160L/200L HE, TP80 FAST Picker (Discontinued — Food Flash: TS2 replaces FAST picker), PF3. Rejected triple-import TS2/TX2 duplicates + legacy RX160/RX260 shells (no current OEM PDP). Typed specs from Product-range-EN.pdf V17 + TS2/TX2 leaflets + hygienic-humid / PF3 pages; family keys `staubli-robotics:{ts2|tx2|tx2-he|tx2-medx|tp80|pf3}`; Switzerland; website `https://www.staubli.com/global/en/robotics.html`. **All 19 imageless** — Stäubli imprint forbids website image republication without written authorization (same pattern as MiR/Yamaha). Approve allowlist empty. Left `pending_review`.
- Wuxi BEWIS Sensing Technology LLC (1225) cleared: rejected all **62** `pending_review` as `non_robot` (inclinometers/compasses/IMUs/gyros/pressure scanning valves — OEM sensor components used on robots/AGVs/UAVs, not robot platforms). Reason: `non_robot: BEWIS/BWSENSING inertial/attitude sensor component …`. Final **0 published / 62 rejected / 0 pending**. Sample IDs: 2795, 3597, 4997, 5038. Website left `https://www.bwsensing.com/`; company notes note not to re-enrich as robots. Did not touch MUKA (1480).
- Stakeholder approved Bühler Group (1507) entire set; API verified **10 published / 0 pending**. Moved to Cleared. Skipped MUKA (1480) — other agent. Next: Wuxi BEWIS Sensing (1225) — sensor components, not robots.
- Bühler Group (1507) curated full enrichment via `fix_buhler_1507_robots.py`: 10 SORTEX/SPARK optical sorters; company website → `https://www.buhlergroup.com/` + Switzerland; Available(11); family keys `buhler:sortex-*` / `buhler:spark`; brochure-backed typed specs where columns agree; CDN **8/8** distinct heroes. Approve allowlist: **5069, 5070, 5071, 5073, 5074, 5075, 5076, 5077**. Hold **5068** (R500 5 IMAGE TO-DO) + **5072** (SPARK Pro IMAGE TO-DO; Pro+ only). Moderation dry-run **8 publish / 2 hold**. Also fixed local `bulk_import_media` empty+`replace_media` clear path (needs deploy for imageless detach without staff session). Tests: **7 passed**. Left `pending_review`.
- REEMAN (1421) curated full enrichment via `fix_reeman_1421_robots.py`: mapped CN↔EN catalog, kept **22** EN-named SKUs, rejected **12** language/SEO/chassis duplicates; company website → `https://reemanbot.com/`; China + Available(11); family `reeman:{series}`; CDN verify **10/10** distinct heroes. Approve allowlist: **2305, 2316, 2326, 2330, 2332, 2334, 4658, 4659, 4661, 5083**. Hold 12 imageless (forklift banners, food-delivery callouts, Big Dog/Chengying/R1D1/HUSSAR PRO factory/Moon Knight). Tests: **5 passed**. Left `pending_review` (no auto-publish).
- Stakeholder confirmed Hyundai Robotics (49) fully done; API verified **12 published / 11 rejected / 0 pending**. Moved to Cleared. Next queue target: REEMAN (1421) — **34 pending_review**.
- Company **49 (Hyundai Robotics)** held pending clearance applied via `fix_hyundai_held_robots.py`: rejected **6** (HH050 alias 3719; industry/7 packages 3722–3726); fixed **5** remaining pending (3709→HC3303B Series with fresh product-API hero; HDC25-18 `thumb_prod_hdc_01.png`; HDC35-18 `thumb_hdc_applied_01.png` with distinct content hash; LABOT + Robot Barista fresh `robot-solution/page` notice heroes). Robot Barista signed S3 URL exceeded `Robot.image` varchar(500) — used short `/api/v1/file/ck/view/{fileSeq}` for import. CDN HTTP 200 + image body verified for all five; HDC25≠HDC35 hashes. Still held: **0**. New approve allowlist pending IDs: **3709, 3710, 3711, 3729, 3730**. Published set untouched: 3731, 3721, 3720, 3718, 3717, 3716, 3712.

## 2026-07-21

- Staged exact official Huayan renders for all **26** imageless current models as external, non-primary `review_required` candidates for the new image-rights workflow. The mutation-free validator confirmed 26/26 HTTPS image bodies, decoded dimensions, live robot identities/status/media, strict backend enums, and unique SHA-256 hashes with no collisions; unresolved-rights confidence is capped at 69. Production was not modified because its current photo serialization still omits `rights_status` on 61 published photos. Tests: **20 passed**. Files: `stage_huayan_1490_image_candidates.py`, `staging/robots/huayan-robotics/image-rights-candidates.json`, and `staging/reports/huayan-image-candidate-validation.json`.
- Stakeholder approved Huayan Robotics' nine image-backed current models; API verified company totals **18 published / 27 pending**. The remaining 26 current models are held for media rights rather than simple discoverability: Huayan product pages, brochures, downloadable STEP assets, and some publisher/user posts contain product imagery, but no open commercial license or written republication permission was found; Wikimedia has no matching reusable Huayan media. Legacy S35 `3679` is the separate 27th pending record. Permission contacts: `marketing@huayan-robotics.com` and `marketing.oversea@huayan-robotics.com`.
- Stakeholder approved YASKAWA Electric / Motoman company 772; API verified **75 published / 0 pending**. Post-approval verification retained full coverage for payload, reach, family metadata, manufacturer country, and valid features on 75/75 robots, with weight and repeatability on 67/75. Moved the company from Big fleets to Cleared.
- Huayan Robotics (1490) Task 5 production reconciliation completed with the supported apply command. The script detail-PATCHed and verified all **42 existing current model records**, created **0**, and reported **0** partial writes, blockers, or errors. Before/after status totals remained **36 pending_review / 9 published** across all 45 company records; S35 `3679`, Echo shell `5302`, and STAR shell `5303` remained untouched. The post-apply dry-run verified **42 current / 0 missing / 0 unexpected**, exact factual/family/taxonomy/tag/source/typed-spec state, 26 imageless records with 26 valid actionable licensing notes, no Huayan media ingestion, and one accepted E10L-Pro official-video follow-up. Existing media and complete serialized video row sets were preserved by per-record invariants. Moderation dry-run: **10 publish / 0 reject / 26 hold**; legacy S35 is the extra publish candidate and remains excluded from the nine-current-model approval allowlist. Verification: **46 tests passed** and `py_compile` completed cleanly. Report: [Huayan 1490 Production Apply](../staging/reports/huayan-1490-production-apply.md).
- Huayan Robotics (1490) full catalog reconciliation applied and verified: enriched all 24 existing current model records and created 18 missing model-level records (`5559`–`5576`) for a complete **42-model** current catalog. Preserved all existing statuses and media; final live state is **9 published / 36 pending** plus legacy S35 and two published family-shell retirement candidates. Current Chinese S-series values supersede documented stale English conflicts. No reusable Huayan media license was found, so **26 imageless records** carry explicit licensing blockers; the 18 new records remain pending with no copied/hotlinked media. Nine image-backed pending current models are eligible for approval (`3673`–`3677`, `3680`–`3683`). Verification: **31 tests passed**, 42 current / 0 missing / 0 unexpected, 26/26 licensing notes, and all 19 attached CDN images returned HTTP 200. Scripts: `huayan_1490_catalog.py`, `fix_huayan_1490_robots.py`; reports: `staging/reports/huayan-1490-full-enrichment.json`, `staging/reports/huayan-1490-cdn-verification.json`.
- Company **174 (NASA)** "fix error/warning flags" — turned out to be a **company MIS-ATTRIBUTION**, not a flag problem. Of 26 robots only **Perseverance Rover (1235)** is NASA; the other **25 were Xiaomi (24: CyberOne, CyberDog, CyberDog 2, "Xiaomi Robotics", 20 robot vacuums) or Noetix (1: N2)** with `mi.com` / `noetixrobotics.com` URLs — which is what raised `url_domain_mismatch` on all 25. Xiaomi (**#115**) and Noetix (**#135**) already existed and were **empty (0 robots)**, so this was a clean move with nothing to dedupe. Per stakeholder: **re-parented all 25** (`reparent_nasa.py`) via the serializer's `company`-string path — it `get_or_create`-matches the EXISTING company by exact name (verified on one robot first: 4798 → #115, **no duplicate company created**); each write re-fetched and asserted `company_ref.id`. Result **25/25, 0 failed**; NASA now holds only Perseverance, Xiaomi 24, Noetix 1, and **all 25 url_domain_mismatch errors are cleared**. (The company-robots LIST endpoint served stale Redis cache showing NASA=26 afterwards; per-robot `company_ref` is ground truth and was correct.) Then enrichment (stakeholder chose published **+** rejected): **Perseverance** +2 official NASA/JPL videos; **CyberOne/CyberDog/Noetix N2** got uses+movement+industries+tags+release_year (2022/2021/2025), and CyberDog's **6 dead gallery photos** were purged (5 healthy kept). **20 vacuum stubs** batch-enriched (`fix_xiaomi_vacuums.py`): uses=cleaning, movement=wheeled, industries=homes+cleaning, tags, **country USA→China** (wrong, inherited from the NASA bucket), task `purpose` (also fixing 5 `purpose==description` exacts), `battery_capacity` from scraped mAh (clears missing_specs) + suction Pa appended to features. **16 verified product heroes** attached (`attach_xiaomi_vac_heroes.py`, external mi.com → force copy-media). **Trap: mi.com `og:image` is a LOGO TRAP** — for S40/6 Max/S40C the og:image is the orange **MI logo**, not the product; a 4797 candidate was a **TÜV certification badge**. Heroes were therefore picked visually per model from the page image list, never blindly from og — final contact-sheet check confirms all 16 are genuine vacuum shots, 0 logos. **Final: NASA 1/1 clean; Noetix 1/1 clean; Xiaomi 24 with 0 purpose-dupes, 0 missing uses, 0 missing country, 0 off-domain URLs.** **Open: 6 rejected Xiaomi robots still imageless** (2528 S20+, 2529 S20, 4795 E5, 4797 X10, 2660 CyberDog 2, 4362 "Xiaomi Robotics" = a CyberOne duplicate) — mi.com blocked/served no usable product image; left imageless rather than attaching a logo/badge/sibling image. NOTE: prod `verify_content` + prod-DB flag audit **could not run — gcloud/kubectl token still expired**; verification done via research API + content-hash + visual contact sheets.
- Approved the Huayan Robotics company 1490 full-enrichment design: reconcile 24 existing current model records plus 18 missing models to Huayan's 42-model official catalog, preserve existing moderation states, and flag legacy S35 plus published Echo/STAR family shells for separate retirement decisions. No reusable image license was found, so the design forbids new image ingestion and requires explicit licensing blockers on imageless records. Added [Huayan Robotics 1490 Full Enrichment Design](specifications/huayan-1490-full-enrichment-design.md).
- Added the Huayan Robotics company 1490 implementation workflow: TDD-defined 42-model catalog contract, exact official model tables, idempotent 24-update/18-create reconciliation, production dry-run review gates, status invariants, and fail-closed media licensing. See [Huayan Robotics 1490 Full Enrichment Implementation Plan](plans/huayan-1490-full-enrichment-plan.md).
- MiR (370) catalog identity/specification pass completed: retained seven canonical MiR products; rejected two duplicate MiR1000 rows, duplicate MiR500, and invalid “MiR500 Shelf Carrier”; reparented third-party MC250/MC600 to new canonical Enabled Robotics company 1532 as ER-FLEX/ER-MAX with exclusive ownership. Applied exact OEM/distributor-backed specs, lifecycle status, family/taxonomy/source metadata, and corrected videos. No approval recommendation was issued: Teradyne terms restrict materials to noncommercial informational use, while Enabled Robotics requires written approval for image copying. Four MiR records remain deliberately imageless; three MiR and two Enabled pending galleries still require authenticated detachment. Script: `fix_mir_370_robots.py`; tests: **4 passed**.
- Stakeholder approved CTRL Robotics company 782; API verified **1 published / 5 rejected / 0 pending** and moved the company to Cleared.
- CTRL Robotics (782) ownership cleanup completed. CTRL's official site identifies it as a fleet integrator rather than the hardware OEM, so the queue now retains only its current branded **Box 5086** offering. Enriched Box from its exact current page with four compartments, 140 L, 40 kg, 1 m/s, 1.5-hour charging, and 6–8-hour runtime; rebuilt the official 701×501 product image as one exact 1200×858 owned-CDN hero. Reparented original PuduBot `5085` to Pudu Robotics and REEMAN WBOT11B/FBOT13B chassis `5083`/`5082` to REEMAN, replacing the stale CTRL secondary ownership relation with exclusive OEM ownership. Rejected exact duplicate R1D1/KettyBot/BellaBot/LuckiBot rows and unverifiable dead Kiki. CTRL moderation: **1 publish / 0 hold**; tests: **3 passed**. Script: `fix_ctrl_782_robots.py`.
- Stakeholder approved Bluesword company 997; API verified **9 published / 8 rejected / 0 pending** and moved it to Cleared.
- **Correction — Yamaha 1484 image rights:** CADENAS terms prohibit public redistribution of the exact configured CAD renders, and Yamaha's site terms do not grant commercial republication rights for catalog illustrations. Seven pending F/B records are blocked again; published XBOT `4385`, JGX16-H `4392`, and JGX16-V `4393` also carry known catalog-derived uploads. Cleanup dry-run identified all ten exact uploaded objects, but no production record changed: S3 deletion returned `AccessDenied`, admin photo deletion returned `403`, and published records remain protected by the no-overwrite guardrail. Added a mandatory republication-rights gate and converted `fix_yamaha_lcmr200_images.py` into a fail-closed cleanup tool. Report: `staging/reports/yamaha-lcmr200-image-rights-cleanup.json`.
- Bluesword (997) full catalog curation completed: retained **nine named robot products** (Jaguar, Four-Way Pallet Shuttle, Asian Elephant, Kangaroo, Spider, UnitLoad-Libra, Gecko, Transfer FMR, and Latent Mobile Robot) and rejected eight fixed-infrastructure/category/family/phantom rows. Replaced all keeper galleries with one exact official hero each, including four missing/placeholder images and third-party media; distinct owned-CDN hashes **9/9**. Applied current OEM names, exact PDP/brochure specs, family/taxonomy/source metadata, and exact or relevant official videos. Kept Four-Way Shuttle's conflicting 1.5/1.6 t standard payload untyped. Moderation: **9 publish / 0 hold**; tests: **3 passed**. Script: `fix_bluesword_997_robots.py`.
- Extended Yamaha LCMR200 image research to Yamaha's official configurable CAD catalog on CADENAS. It exposed distinct 3D renders for every F/B length and cable-entry selection, resolving seven pending rows (`3325`, `3326`, `4386`, `4387`, `4388`, `4389`, `4391`); B5 `4390` was already published and was not overwritten. Replaced all seven text-heavy banners, preserved `pending_review`, and verified seven valid owned-CDN image bodies with distinct SHA-256 hashes. Yamaha now has **8/10** pending records ready; only JGX16-H/V remain held for missing features. Script: `fix_yamaha_lcmr200_images.py`; tests: **6 passed**.
- Stakeholder approved Balyo company 242; API verified all six canonical robots published, then retired legacy published duplicates TUGGY `372` and REACHY `373`. Final state: **6 published / 11 rejected / 0 pending**.
- Yamaha LCMR200 hero repair applied to `4385` XBOT, `4392` JGX16-H, and `4393` JGX16-V. Replaced the shared 1920×672 text-heavy banner with three distinct robot-dominant exact renders extracted from Yamaha’s official LCMR200 catalog; uploads now have one primary photo each, distinct SHA-256 hashes, valid CDN image bodies, and preserved `pending_review` status. Eight F/B length/cable-entry variants remain held: YRG’s eight model-specific links resolve to one byte-identical module PDF that reuses a generic render. Script: `fix_yamaha_lcmr200_images.py`; Yamaha image tests: **8 passed**.
- Balyo (242) full catalog curation completed: reduced **15 pending rows to six canonical robots** (VEENY 2680, REACHY 2681, LOWY CB 2682, LOWY 2683, LOWY HD 2684, TUGGY 2685) and rejected nine exact pending duplicates. Applied exact current PDP specs, configuration caveats, family/taxonomy/source metadata, typed payload/speed/dimensions where safe, exact official videos or relevant official family overviews with documented dead searches. Replaced every keeper gallery with one exact current OEM 1200×900 cover; fixed LOWY HD's wrong LOWY application hero, removed TUGGY's LOWY CB photo, and replaced its wrong REACHY/stacker videos. Owned-CDN hashes are distinct **6/6**; moderation **6 publish / 0 hold**; tests **4 passed**. Legacy published duplicates TUGGY 372 and REACHY 373 are documented for retirement after the canonical records are approved. Script: `fix_balyo_242_robots.py`.
- Stakeholder approved Dobot (Yuejiang) company 1161; API verified **13 published / 0 pending** and the living checklist moved it to Cleared.
- Stakeholder approved 6 of Yamaha’s 17 unique merged records and held the remaining **11 LCMR200** rows because their heroes are text-heavy promotional/specification assets rather than robot-dominant product images. Live state: **45 published / 22 rejected / 11 pending**. Added an explicit reusable gate to both enrichment and moderation skills: primary images must be clean exact-model photos/renders with the robot as the main visual subject; promotional banners, feature/spec cards, brochure panels, infographics, dimensional drawings, CAD/design sheets, and similar text-heavy assets are hard image failures.
- Dobot (1161) queue blocker completed: curated **DOBOT CR 30H (4242)** from the exact OEM PDP and June 2025 launch release. Replaced the blank hero with a visually verified Standard Edition asset cropped from the OEM card and rebuilt at 1108×1200; owned CDN body/hash verified. Added OEM features/applications, family metadata, sources, availability, country/taxonomy, and typed 30 kg payload / 1800 mm reach / 98.5 kg / ±0.05 mm / 6 DOF. Moderation dry-run: **13 publish / 0 hold**. Script: `fix_dobot_1161_robots.py`; tests: **2 passed**.
- Stakeholder merged and removed duplicate Yamaha company 1477 into canonical company 1484. Audited the 39 merged `pending_review` rows by normalized exact model token against the 39 published Yamaha records. Rejected **22 confirmed duplicates** with reason `duplicate: merged from former Yamaha company 1477; keep matching published Yamaha model in company 1484`. Preserved all 17 unique rows in To Review: **14 must-clear pass** for approval and **3 holds** missing features (`4393` LCMR200-JGX16-V, `4392` LCMR200-JGX16-H, `4377` YK400XR).
- Stakeholder approved Gurki (974), Intamsys (1073), Intuitive Surgical (52), 6 River Systems (1373), Auris Health (1511), and Infinium Robotics (783); API verified 0 pending for each (published counts 8 / 5 / 5 / 1 / 1 / 1). inVia (397) audit confirmed published `2977` **inVia Picker robot** duplicates legacy published `272` **inVia Picker**. Keep 2977 because it has the exact PDP, typed specs, family metadata, sources, and curated media; reject incomplete CJK legacy 272. PickerWall 2978 is already rejected.
- Geek+ (1398) full catalog audit completed: **14** exact robots fully enriched, **38** duplicate/solution/configuration/alias shells rejected, and **3** held for exact media (P800H 1785, S100C 1792, RS11-DA 4950). Typed-spec coverage across 17 retained records: payload 16, weight 13, speed 16, dimensions 14–16, runtime 13, battery 15. Exact videos replaced for six models; accepted heroes copied and CDN/pixel verified 14/14. Exact approval allowlist recorded under Partial.
- Yamaha Robotics **1484** media follow-up: replaced the YK1200XG text-heavy 600×254 banner with Yamaha's clean 1000×618 launch-release product photo. Rebuilt the only available exact-model YK400/610/710XEC OEM PDF renders as card-sized 1200 px transparent assets; retained the suitable 1000×1016 YK510XEC launch photo. All five primary hashes are distinct, owned CDN verification is HTTP 200 with valid image bodies **5/5**, and moderation dry-run is **5 publish / 0 hold**. Script: `fix_yamaha_1484_robots.py`; regression tests: **4 passed**.
- Full curated enrichment completed for Yamaha (1484: 5 remaining pending), Gurki (974: 8), inVia Picker (397: 1), and 6 River Chuck (1373: 1). All citeable typed specs applied; exact-model media copied and CDN/hash verified; videos curated or exact-model dead searches recorded. Yamaha’s four XEC video sets were corrected with `replace_videos=True` + `patch_existing=True`, leaving one official Yamaha YK-X overview each; 2 regression tests pass. Rejected inVia PickerWall as `non_robot_workflow` and Plus One PickOne as `non_robot_software`; Plus One (254) now has 0 pending.
- Curated full passes applied to **Hyundai Robotics (49)**, **DELTA Electronics Shanghai (1206)**, and **Mujin (810)**. Clean pending subsets: Hyundai 7, DELTA 9, Mujin 8; exact approval allowlists recorded in the checklist. Rejected five Hyundai generic category/series duplicates and one Mujin language duplicate. Held unresolved package/category records, aliases, identical-byte sibling heroes, undocumented R65 suffixes, and records without standalone OEM PDPs rather than inventing specs or substituting media. CDN verification passed 7/7, 9/9, and 8/8 for enriched subsets.
- Full curated enrichment completed for **Intamsys (1073: 5)**, **Intuitive Surgical (52: 3)**, **Auris Health (1511: 1)**, and **Infinium Robotics (783: 1)**. Applied OEM PDP/documentation, FDA records or whitepapers where relevant, full family/taxonomy/source metadata, all citeable typed specs, exact-model media with CDN HTTP verification, curated videos, and explicit dead searches where public physical specs or OEM videos were absent. All 10 remain `pending_review`; moderation dry-run: 10 publish / 0 hold.
- **Correction:** the Needs-cleanup overnight runner performed only moderation gate-filling, not the full enrichment required by `content-queue-robot-backfill` (OEM PDP + datasheet/technical-guide search, all citeable typed specs, media/hash validation, videos/tags, and documented dead searches). Retracted its readiness recommendation and returned 13 companies to Needs cleanup while curated full passes run.
- Stakeholder approved 10 companies; API verified **0 pending** for each: Freefly Systems (1452: 4 published), Dusty Robotics (1510: 1), Flytrex (1448: 2), Liquid Robotics/Boeing (429: 1), Asensus Surgical (328: 2 published + 1 rejected + 1 draft), Harvest Automation (1502: 1), Diversey/Solenis (367: 3), Auris Health/J&J (383: 3 published + 1 rejected), Globus Medical (432: 1), Shift Robotics (856: 2). Moved all to Cleared in `checklists/approve-publish-status.md`.
- Companies **406 (Briggo / Costa Coffee)** and **1450 (Formic Technologies)**: replaced all nine IMAGE TO-DO slots with visually verified, model-specific OEM assets from Costa's Smart Café page and current Formic solution pages. Content hashes are distinct 9/9; copy-media succeeded; owned CDN HTTP verification is Briggo 1/1 and Formic 8/8. Smart Café use taxonomy corrected to `dispensing`. Moderation dry-run: **9 publish / 0 hold**. Script: `fix_briggo_formic_images.py`.

## 2026-07-20

- Company **383 (Auris Health / J&J)**: rejected CJK **Monarch 平台 (1653)** dupe of **1648**; enriched **MONARCH™ Platform (1648)** + **MONARCH™ QUEST (1649)** — US; Available; family `auris:monarch`; OEM J&J MedTech bronchoscopy + QUEST FDA PR (Mar 2025); EN purpose/features/sources. Soft: typed mass/dims/MSRP absent on PDP. Script: `discover_auris_robots.py`. → Approve tonight.
- Company **432 (Globus Medical)**: enriched **ExcelsiusFlex (4959)** — US; Available; family `globus:excelsiusflex`; OEM PDP + FDA K240721 (Jul 2024 TKA); EN purpose/features/sources. Soft: mass/reach/MSRP not public. Script: `discover_globus_robots.py`. → Approve tonight.
- Company **856 (Shift Robotics)**: EN rename + enrich CJK twins → **Moonwalkers (4027)** + **Moonwalkers Aero (577)** — US; Available; family `shift:moonwalkers`; OEM PDP specs (7 mph→11.27 km/h; 5.2/4.2 lb; user 100 kg; $1399/$999); company website cleaned. Soft: Sold out on shop scrape. Script: `discover_shift_robots.py`. → Approve tonight.
- **Needs cleanup overnight** (Bluefin 160 already Cleared): rejected ACY (1369) pending EOAT ×55 + Jiangsu DINGS Gripper (5209) + AGV Network (615); soft-enriched remaining Needs-cleanup fleets (Geek+ 55 / Yamaha 39 / Hyundai 23 / DELTA 17 / Mujin 11 / Gurki 8 / Intamsys 5 / Intuitive 3 / inVia 2 / 6 River / Auris / Infinium / Plus One) — families, uses, `manufacturer_countries`, Available. Geek+ HubSpot heroes → owned CDN **55/55** (content-queue copy-media + `X-Internal-Secret`). Soft: no deep datasheet re-scrape; Hyundai series shells remain. Report: `docs/reports/needs-cleanup-morning-report.md`. Script: `overnight_needs_cleanup_enrich.py`.
- Company **772 (YASKAWA Electric / Motoman)**: full soft enrich on all **75** pending — Japan; Available; 14 families `yaskawa:gp|hc|nex|pl|mpp|sg|motomini|mh|ph|mys|ar|ga|sp|mpx`; Motoman Spec Finder **payload+horizontal reach** ×75; datasheet PDF **weight+repeatability** ×67; Motoman-prefixed names → clean model; wiped shared nav-chrome features; purpose from OEM applications. Soft: MYS/NEX/MPX datasheet weight gaps; MH900 mass 10 000 kg per datasheet; MPP3S retained prior 130 kg (no PDF). **must_clear_pass** ×75; CDN 75/75. Script: `discover_yaskawa_robots.py`. → Big fleets bulk approve.
- Company **1444 (Draganfly Innovations)**: soft-enriched **4** pending UAVs — Apex (5102) / Commander 3XL (5101) / Heavy Lift (3616) Available + Tango2 (2819) Discontinued — US; families `draganfly:apex|commander-3xl|heavy-lift|tango2`; OEM payloads/dims/runtime/MSRP; CDN heroes HTTP 200. Soft: multirotor empty weights absent. **must_clear_pass** ×4. Script: `discover_draganfly_robots.py`. → Approve tonight.
- Company **1454 (Glydways)**: soft-enriched **Glydcar (2878)** — US; Announced (2026); family `glydways:glydcar`; ATN purpose/features; OEM viz hero CDN 200. Soft: dims/weight/speed/MSRP not public. **must_clear_pass** ×1. Script: `discover_glydways_robots.py`. → Approve tonight.
- Company **1415 (Nitto Seiko America)**: soft-enriched **5** pending industrial screw drivers — SR375 (4239) / SR580 (3183) / SR580 Vision (3184) / SR780 (3185) / PD400UR (2204) — US; families `nitto:sr375|sr580|sr780|pd400`; OEM US + JP NS-056 / PD400 specs; 4239/3183/2204 CDN 200; **3184/3185 IMAGE TO-DO** (OEM img 404). Soft: PD400UR is UR+ EOAT. **must_clear_pass** ×3 (+ 2 imageless). Script: `discover_nitto_robots.py`. → Approve tonight.
- Company **1452 (Freefly Systems)**: soft-enriched **4** pending — Alta X Gen2 (2875) / Alta X (5169) / Astro Max (2876) Available + Alta 8 Pro (5170) Discontinued — US; families `freefly:alta-x|astro|alta-8-pro`; OEM freeflysystems.com specs; replaced Alta X chart CDN + wrong Astro cinema CDN. Soft: Gen2 empty shared Alta X airframe; MSRP not public. **must_clear_pass** ×4. Script: `discover_freefly_robots.py`. → Approve tonight.
- Company **1510 (Dusty Robotics)**: soft-enriched **FieldPrinter 2 (5107)** — US; Available; family `dusty:fieldprinter`; 23 lb / 10.4 kg; OEM dustyrobotics.com (not parked dustrobotics.com). Soft: dims/speed/MSRP absent. **must_clear_pass** ×1. Script: `discover_dusty_robots.py`. → Approve tonight.
- Company **1448 (Flytrex)**: soft-enriched **Sky2 (3646)** Available + **Sky (2866)** Discontinued — US; family `flytrex:sky`; Sky2 8.8 lb; Sky 6.6 lb + FAA FTX-M600P dims/speed; cleared junk Sky CDN → IMAGE TO-DO. Soft: flytrex.com JS shell. **must_clear_pass** ×1 (+ imageless Sky). Script: `discover_flytrex_robots.py`. → Approve tonight (Sky2); Sky waits on hero.
- Company **367 (Diversey / Solenis)**: enriched **TASKI Ecobot 40 (5148)** + **Ecobot 50 Pro (5147)** — US; Available; shared family `taski:ecobot`; real TASKI PDPs (not solenis.com home); OEM dims/weight/speed; CDN heroes HTTP 200. Soft: uptime ranges not typed. Script: `discover_taski_ecobot_robots.py`. → Approve tonight.
- Company **406 (Briggo / Costa Coffee)**: enriched **Smart Café (4419)** — US; Available; family `briggo:smart-cafe`; Costa business OEM URLs (Briggo acquired 2020); 3′×3′ footprint typed. Soft: IMAGE TO-DO (Costa SPA timeout). Company website → `us.costacoffee.com/costa-business`. Script: `discover_briggo_robots.py`. → Approve tonight.
- Company **1450 (Formic Technologies)**: enriched all **8** pending Automate NOW solutions — company website `formic.co` + US `country_id`; families `formic:humanoids|machine-tending|palletizing|amr|pallet-wrapping|case-packing`; AMR payload 1500 kg; Top Load 9.07 kg + 8′ cell dims. Soft: IMAGE TO-DO (shared OG only). Script: `discover_formic_robots.py`. → Approve tonight.
- Company **1502 (Harvest Automation)**: enriched **HV-100 (4979)** — US; Available; family `harvest-automation:hv-100`; OEM datasheet dims/weight/payload/runtime; company website → `https://harvestai.com/`. Soft: length/MSRP not public. **must_clear_pass** ×1. Script: `discover_harvest_robots.py`. → Approve tonight.
- Company **805 (Canvas Technology / Amazon)**: rejected **Canvas AMR (3612)** — `phantom_sku` (Amazon acquired 2019, shuttered 2022; no public SKU); cleared wrong Instructure Canvas LMS website. Script: `_reject_canvas_amr.py`.
- Company **1374 (ADLINK Technology)**: **SKIPPED** US overnight — Taiwan HQ (Taoyuan); 43 pending mostly edge COM/SBC/DLAP/ROS controllers. Soft-note only; marked done/processed. Script: `_skip_adlink_1374.py`.
- Company **429 (Liquid Robotics / Boeing)**: deep-enriched **Wave Glider (3869)** — US; Available; family `liquid-robotics:wave-glider`; OEM SV3/SV5/SV6 comparison specs in features; typed single length/payload blank (family table). Soft: MSRP not public. Script: `fix_liquid_asensus_robots.py`. → Approve when ready.
- Company **328 (Asensus Surgical)**: deep-enriched **Senhance® (1635)** + **ISU (4032)** — US; Available; families `asensus-surgical:senhance|intelligent-surgical-unit`; OEM + fact sheet/brochure; rejected CJK dupe **1654**. Soft: curb weight/DOF/list price not on public pages. Script: `fix_liquid_asensus_robots.py`. → Approve when ready.
- Company **1415 (Nitto Seiko America)**: soft-enriched 5 screw-driving SKUs (SR375/580/780 Yθ + PD400UR) — US; families `nitto:sr-y-theta|pd400`; OEM heroes; **must_clear_pass** ×5. Script: `discover_nitto_robots.py`. → Approve when ready.
- Company **174 (NASA)**: rejected **22** misfiled Xiaomi vacuum / CyberDog pending rows → **Cleared** (0 pending). Script: `_reject_nasa_xiaomi.py`.
- Overnight US drain started: soft-pass `overnight_us_discover.py` on 9 explicit-US OEMs; deep curated agents in flight; morning report: `docs/reports/us-overnight-morning-report.md`.
- Company **160 (Bluefin Robotics / GDMS)** stakeholder approved 3 → **Cleared** (4 published / 0 pending; BF-21 dupe 5044 rejected).
- Company **160 (Bluefin Robotics / GDMS)**: soft-filled published **Bluefin-21 (197)** EN + specs; rejected pending dupe **5044**; enriched **Bluefin-9 (3607)**, **Bluefin-12 (5043)**, **Bluefin HAUV (3608)** — US; Available; families `bluefin:9|12|21|hauv`; OEM GDMS dims/weight/endurance; speed knots→km/h; OEM product heroes on CDN. Soft: MSRP not public. **must_clear_pass** ×3. Script: `discover_bluefin_robots.py`. → Approve tonight.
- Company **70 (Lumos Robotics)** stakeholder approved → **Cleared** (5 published / 0 pending; LUD + LUS 2 + MOS 2 + NIX S3 + Touch R1).
- Company **70 (Lumos Robotics)**: full soft enrich on all **5** pending — China; Available; families `lumos:lud|lus|mos|nix|touch`; OEM PDP specs (payload/DoF/dims/speed/torque/reach); EN purpose/features; LUS 2 hero refreshed from OEM `lus2/front.webp` (was thin 32 KB CDN). Soft: no public MSRP; Touch R1 weight not on PDP. **must_clear_pass** ×5; CDN HTTP 200 ×5. Script: `discover_lumos_robots.py`. → Approve tonight.
- Company **212 (SMP Robotics)** stakeholder approved 13 → **Cleared** (15 published / 0 pending incl. soft-filled Argus S5.2 + Bird Control).
- Company **1461 (Jaten Robot)**: restored soft-rejects **5185** SDM300-339-MGD + **5190** MN30-164 → **published**. **5190** hero swap (tiny 6.7 KB primary → gallery product render). Soft: China, families `jaten:sdm300` / `jaten:mn`. Kept **5** `*-335-MG0` phantoms rejected (shared 404 OEM image). Queue **9** published / **0** pending / **5** rejected. Scripts: `restore_jaten_5185_5190.py`, `_jaten_force_restore.py`.
- Company **212 (SMP Robotics)**: soft-enriched **13** pending S-series configs + published **Argus S5.2 (5250)** / **Bird Control (5252)** — US; Available; families `smp:s5-argus|s2-gas-monitor|s3-inspector|s4-bird|s6-gas-leak|s7-delivery|s8-mosquito|s11-telepresence`; shared OEM 2021 platform specs (4–6 km/h → `speed` 6.0, 1420×780×1750 mm, 110 kg); EN rewrite + sources. Soft: option payloads (ALPR module, trap capacity, cargo kg) not typed; S5 HD/PTZ/Thermal/Perimeter/Solar/SmartHouse/ALPR kept as distinct Argus configs (OEM pages). **must_clear_pass** ×13. Script: `discover_smp_robots.py`. → Approve tonight.
- Company **147 (Ekso Bionics)** stakeholder approved 5 → **Cleared** (5 published / 0 pending; Indego dupe 436 + family shell 179 rejected).
- Company **807 (VisionNav Robotics)** stakeholder approved all → **Cleared** (**16** published / **0** pending; VNE/VNK/VNP/VNQ/VNR/VNSL/VNST/VNT fleet).
- Company **1369 (ACY Automation)** stakeholder: catalog is **EOAT accessories, not robots**. Moved off Big fleets → Needs cleanup. Pending **55** should reject as `non_robot` (same as Flexiv Grav). **35 already published** — awaiting reject-or-keep.
- Company **147 (Ekso Bionics)**: rejected **Indego Personal (436)** dupe of **1967** + phantom **EksoNR / EksoGT (179)**; soft-enriched **EksoNR (1966)**, **Ekso Indego Personal (1967)**, **Ekso EVO (437)** Available + **EksoGT (2481)** / **EksoVest (1968)** Discontinued — US; families `ekso:eksonr|indego|evo|eksogt|eksovest`; NR ~24.95 kg / Indego ~14 kg; EN rewrite on EVO; GT factory-misdesc fixed. Soft: Indego Therapy not a separate pending row; past-product heroes may be sparse. **must_clear_pass** ×5. Scripts: `discover_ekso_robots.py`, `_fix_ekso_finish.py`. → Approve tonight.
- Company **1378 (Accuray Inc.)** stakeholder approved 4 → **Cleared** To Review (**0** pending; **6** published: Radixact System + TomoTherapy Hi-Art/HD/HDA + keepers CyberKnife S7/M6). Trademark shells **1477/1478** rejected.
- Company **1369 (ACY Automation)** orphan-CDN repair: **14** pending had owned CDN `image` with empty `s3_image` and **missing S3 object** (approve blocked “no S3 copy”). Re-imported from live OEM + force copy-media — includes **1402**. Script: `fix_acy_orphan_cdn.py`.
- Company **1378 (Accuray Inc.)**: rejected pending **CyberKnife® S7™ (1477)** / **M6™ (1478)** as dupes of published **222/368**; soft-filled published CyberKnife EN+URL+sources+family; enriched **Radixact System (1479)** Available + **TomoTherapy Hi-Art/HD/HDA (1480–1482)** Discontinued — US; families `accuray:cyberknife|radixact|tomotherapy`; sources set. Soft: OEM Cloudflare-blocked; TomoTherapy stock heroes may be shared. **must_clear_pass** ×4. Script: `discover_accuray_robots.py`. → Approve tonight.
- Company **814 (Dephy)** stakeholder approved Sidekick Starter Pack → **Cleared** (2 published / 0 pending).
- Company **1369 (ACY Automation)** gallery cleanup: ~4 identical images per robot were CDN width-variant thumbs imported as separate photos; Angular Gripper DA heroes were dimension drawings. Fleet now **1 photo each**, no thumb rows; angular → OEM product photos on CDN. Script: `fix_acy_gallery_cleanup.py`.
- Company **814 (Dephy)**: soft-fill — Starter Pack **price_min/max $4,500 USD** (was written as inert `price_usd`); published Sidekick (354) got OEM **url** + `information_source_urls` + EN rewrite + 1.4 kg. Lesson: API price fields are `price_min`/`price_max`/`price_currency`; soft warns still get filled when data exists.
- Company **814 (Dephy)**: soft-patched **Sidekick Starter Pack (5088)** — US; Available; family `dephy:sidekick` with published Sidekick (354); retail kit $4,500; weight ~1.4 kg. Soft: wellness (not medical device) per OEM. **must_clear_pass** ×1. Script: `discover_dephy_robots.py`. → Approve tonight.
- Company **423 (Blue River Technology)** stakeholder approved → **Cleared** (3 published / 0 pending).
- Company **213 (Cobalt Robotics)**: published CJK shell **399** → **Cobalt Security Robot** + EN features/purpose/family `cobalt:security-robot`; rejected pending dupe **1659** → **Cleared**.
- Company **423 (Blue River Technology)**: soft-patched **See & Spray Ultimate (5042)**, **See & Spray (380)**, **LettuceBot (381)** — US; families `blueriver:see-spray|lettucebot`; EN rewrite; Ultimate Available + ~16 mph; LettuceBot Discontinued. Soft: `spraying` use key missing. **must_clear_pass** ×3. Script: `discover_cobalt_blueriver.py`. → Approve tonight. Skipped Brightpick (375) — Czechia HQ, not US drain.
- Company **114 (XRobotics)** + **851 (Foundation Robotics)** stakeholder approved → **Cleared**.
- Company **114 (XRobotics)**: soft-patched **xPizza Cube (5289)** — US; Available; family `xrobotics:xpizza-cube`; OEM dims 508×787×711 mm / 76 kg; features from spec page; logo primary → closed product hero. Soft: lease/MSRP not typed. **must_clear_pass** ×1. Script: `discover_xrobotics_foundation.py`. → Approve tonight.
- Company **851 (Foundation Robotics)**: soft-patched **Phantom (2883)** — US; Available (was Released); family `foundation:phantom`; payload 40 kg / height 1.8 m / 29 DoF / 6.12 km/h; scrubbed junk Drone/Care tags; logo → full-body OEM still. Soft: alternate on-page ~20 kg payload line. **must_clear_pass** ×1. → Approve tonight.
- Company **236 (Piaggio Fast Forward)** stakeholder approved → **Cleared** (3 published / 0 pending; gitamini + gita plus + Grogu™ gitamini).
- Company **236 (Piaggio Fast Forward)**: linked published **Grogu™ gitamini (3768)** into family `piaggio:gita` (same as gitamini/gita plus); variant `gitamini-grogu` / label Grogu.
- Company **342 (iRobot)** stakeholder approved remaining To Review → **Cleared** (31 published / 0 pending; 17 rejected). Includes verify-chip fixes (Combo i5 / 105 / 105X / i4); **j9 (2031)** published alongside j9+ (2029).
- Company **236 (Piaggio Fast Forward)**: soft-patched **gitamini (3767)** + **gita plus (3765)** — US; Available; family `piaggio:gita`; purpose task lines (was desc duplicate); real features (was shop-nav scrape); OEM typed specs (20/40 lb, 28/50 lb curb, 6 mph, dims, runtime); scrubbed junk Humanoid/Drone tags. Soft: `companion` use key missing in taxonomy. CDN studio heroes kept (HTTP 200). **must_clear_pass** ×2. Script: `discover_piaggio_robots.py`. → Approve tonight.
- Company **198 (Bear Robotics)** stakeholder approved → **Cleared** (11 published / 0 pending).
- Company **198 (Bear Robotics)**: soft-patched all **10** pending — Servi Plus/mini/Q, Servi Clean, Carti 100, Carti Low-Profile hub, Carti 400/600/1000/1500 — US; Available; families `bear:servi|carti|carti-lp|servi-clean`; features ≥40; purpose task lines; payload from model designation (Carti) / ~88 lb Servi Plus. Soft: no curb weight on Servi mini / Carti LP hub / Carti 600; Servi Clean Max not a separate DB row. CDN heroes HTTP 200 (distinct). **must_clear_pass** ×10. Script: `discover_bear_robots.py`. → Approve tonight.
- Company **1369 (ACY Automation)**: soft-enriched all **90** pending EOAT SKUs — Taiwan; Available; **24** `acy:*` families from OEM hubs on `https://acy.com.tw/` (not dead `/en/` paths); application **purpose** lines; company website → `https://acy.com.tw/`; misfiled URLs fixed (aluminum profile, HDAC clamp). **Media:** truncated OpenCart primaries (`…/catalog/Quick`) + language PNG stubs → promote working gallery OEM/CDN secondaries + copy-media — **90/90 owned CDN HTTP 200**. Prior `fix_acy_robots.py` was video-only; checklist had wrongly marked cleared after 502 audit. Soft: no year/price; videos on subset. **must_clear_pass** ×90. Scripts: `fix_acy_enrich.py`, `fix_acy_media.py`. → Approve bulk (Big fleets).
- Company **34 (FarmBot Inc.)** stakeholder approved Genesis + Express → **Cleared** (5 published / 0 pending).
- Company **34 (FarmBot Inc.)**: soft-patched **Genesis v1.8 (2760)**, **Genesis XL v1.8 (2761)** Available + **Express v1.0/v1.1 (3571/3572)** Discontinued — US; families `farmbot:genesis|express`; OEM bed dims 1.5×3 m / 3×6 m; agriculture taxonomy. Replaced broken 3 KB shared Genesis placeholders with OEM shop renders on `research-staging/farmbot/…`. CDN HTTP 200; **must_clear_pass** ×4. Script: `discover_farmbot_robots.py`. → Approve tonight.
- Company **7 (Aethon)** stakeholder approved T3/T3 XL/Zena RX/Zena → **Cleared** (4 published / 0 pending). Expanded reviewer-facing `rejection_reason` + `notes` on all **10 rejected**. Content-queue admin API now includes `notes` (UI Notes was N/A because the field was omitted from the payload).
- Company **7 (Aethon)**: enriched live **T3 (1533)**, **T3 XL (1534)**, **Zena RX (1532)**, **Zena (1769)** — US; families `aethon:t3|zena-rx|zena`; T3/T3 XL payloads 340.2 / 453.6 kg + 12 h; OEM studio heroes on `research-staging/aethon/…`. Rejected 3 same-SKU dupes + 5 off-catalog TUG Door/Drawer/Exchange + **TUG (86)** / **TUG T4 (567)** (wrong-brand / non-Aethon heroes; superseded by live catalog). **must_clear_pass** ×4. Script: `discover_aethon_robots.py`. → Approve tonight.
- Company **Epson**: stakeholder merged duplicate **Epson Robots (1445)** into **Epson / Seiko Epson (400)** — **51 published / 0 pending** (14 rejected); company 1445 now 404. Removed 1445 from blocked queue.
- Company **50 (IIT)** stakeholder approved → **Cleared** (11 published / 0 pending).
- Company **342 (iRobot)** verify-flag fix on **5 pending** (API shows 5 To Review, not 7): **3704 Combo i5** — replaced Plus 415X shop copy + wrong lifestyle hero with OEM Combo i5 catalog (`i517020_01`); **5208 / 4734** — OEM master-catalog AutoEmpty Combo heroes; **3157 i4** — junk purpose → task phrase; **2031 j9** — cleared auto-empty contradiction, Discontinued, IMAGE TO-DO (j7 lifestyle gallery could not be deleted without staff session — **Reject in UI** as duplicate of published j9+ **2029**). Script: `_fix_irobot_verify_flags.py`.
- Company **806 (Gecko Robotics)** stakeholder approved TOKA 4 + Flex → **Cleared** (3 published / 0 pending).
- Company **806 (Gecko Robotics)** photo hold fix: **TOKA 4 (4947)** primary → DVIDS **7463870** robot-only hull (no faces; prior REPTX stills with identifiable people removed). **TOKA Flex (3578)** primary → robot-only pipe close-up; OEM tank `scrubber` gallery; text marketing graphic demoted to gallery #3. CDN `research-staging/gecko/*-20260720b.jpg` HTTP 200; **must_clear_pass** ×2. Script: `_gecko_fix_toka_photos.py`. → Approve TOKA 4 + Flex (TOKA 3 already published).
- Company **1424 (SIASUN)** stakeholder bulk approved → **Cleared** (82 published / 0 pending; 64 rejected).
- Company **1479 (EFORT)** stakeholder bulk approved → **Cleared** (67 published / 0 pending).
- Company **806 (Gecko Robotics)**: soft-patched **TOKA 3 (4948)**, **TOKA 4 (4947)**, **TOKA Flex (3578)** — US; Available; family `gecko:toka`; inspection taxonomy. TOKA 3 hero; TOKA 4 logo→Navy DVIDS stills; Flex OEM press (6\" pipe / 275°F). Soft: OEM PDPs folded into Cantilever RaaS. **must_clear_pass** ×3. Script: `discover_gecko_robots.py`. → Approve tonight.
- Company **69 (Locus Robotics)** stakeholder approved Origin/Vector/Array → **Cleared** (4 published / 0 pending).
- Taxonomy warn-fix: **LocusBot (100)** — US country + uses/industries/tags/movement (was empty on published shell). **Seegrid** published rows — filled Palion (251) industries/tags; scrubbed Tow Tractor / RS1 / CR1 junk Humanoid/Drone/Education tags; re-asserted Available on Locus Array/Vector. Script: `_fix_locus_seegrid_taxonomy.py`.
- Company **69 (Locus Robotics)**: soft-patched **Array (2536)**, **Origin (4884)**, **Vector (4885)** — US; Available; families `locus:array|origin|vector`. Origin OEM 36 kg / 518×579×1575 mm / 14 h; Vector OEM 272 kg / 762×565×508 mm / 8–10 h. Heroes on `research-staging/locus/…` (replaced wrong Vector shelf-duo + Array tote graphics). Left published **LocusBot (100)**. CDN HTTP 200; **must_clear_pass** ×3. Script: `discover_locus_robots.py`. → Approve tonight.
- Company **209 (Seegrid)** stakeholder approved Lift RS1/CR1/EL1 AMR + Tow Tractor S7 → **Cleared** (5 published / 0 pending; 3 same-URL dupes rejected).
- Company **209 (Seegrid)**: soft-patched **Lift RS1 AMR (2409)**, **Lift CR1 AMR (2408)**, **Tow Tractor S7 AMR (2312)** — US; Available; OEM payload/speed (RS1 3,500 lb / 5 mph; CR1 4,000 lb / 5 mph + 50×92 in; S7 10,000 lb / 4 mph + 36×76 in). Discovered **Lift EL1 AMR (5558)** (2,500 lb / 3.4 mph; OEM warehouse hero on `research-staging/seegrid/…`). Rejected same-URL dupes **2307 / 2309 / 4420**. Left published **Palion AMR Series (251)** family shell. CDN HTTP 200; **must_clear_pass** ×4. Script: `discover_seegrid_robots.py`. → Approve tonight.
- Company **18 (Boston Dynamics)** stakeholder approved **Stretch (1760)** → **Cleared** (5 published / 0 pending; Atlas 1761 + Spot with Arm 1833 rejected).
- Company **18 (Boston Dynamics)**: soft-patched **Stretch (1760)**, **Atlas (1761)**, **Spot with Arm (1833)** — US country; families `boston-dynamics:stretch|atlas|spot`; Available; Stretch payload 22.68 kg (50 lb OEM); Spot 33.8 kg / 14 kg payload / 700 mm height / 5.76 km/h from OEM spec table; purpose fixes on Spot with Arm. CDN heroes HTTP 200 ×3; **must_clear_pass** ×3. Script: `discover_boston_dynamics_robots.py`. → Approve tonight.
- Company **1479 (EFORT)**: soft-patched all **67** pending — China country (stakeholder); `family_*` on 10 series; OEM application **purpose** (newline-separated) on all 67; typed **payload_kg/reach_mm** on 49 (model designation); cleaned junk uses on GR6150; cobot re-import attempted (OEM only 1 PDP image each → **few_photos** soft on ER C ×5). **must_clear_pass** ×67. Script: `fix_efort_soft.py`. → Approve bulk.
- Company **307 (Trossen Robotics)** stakeholder approved **WidowX AI (5273)** → **Cleared** (10 published / 0 pending).
- Company **307 (Trossen Robotics)** **WidowX AI (5273)**: primary was CDN **403** (looked imageless); re-uploaded OEM arm-from-box hero + 3 gallery stills (`research-staging/trossen/widowx-ai-*-20260720.jpg`); PATCH payload 1.5 kg / reach 700 mm / weight 4 kg / DoF 6 / $4,545.95–$4,995.95 / Available. Primary HTTP **200**. Other 8 already published → **1 pending**. Script: `_trossen_fix_widowx_ai_photo.py`. → Approve WidowX AI only.
- Company **307 (Trossen Robotics)** warn-fix: 4-photo galleries on all 9 pending; OEM prices (Solo / Mobile AI / WidowX AI); drawing years (PX 2018, VX/WX S 2020); Discontinued for PDP-banner SKUs; Available for Mobile AI + WidowX AI. Soft leftover: no price on discontinued pages without MSRP. Scripts: `_trossen_fix_warns.py`.
- Company **307 (Trossen Robotics)**: soft-patched all **9** pending — US country; families `trossen:aloha` / `pincherx` / `viperx` / `widowx`; typed PX100 0.05 kg/300 mm, VX300S 0.75 kg/750 mm, WX250S 0.25 kg/650 mm from Interbotix docs; WidowX AI weight 4 kg. Soft: ALOHA kits no single-arm payload; WidowX AI shares AI-ready arm still with Viper Aloha follower; Widow Aloha Set shares Stationary kit still. CDN HTTP 200 ×9; **must_clear_pass** ×9. Script: `_trossen_soft_patch.py`. → Approve tonight.
- Company **259 (AMP Robotics)** stakeholder approved Delta + Delta Compact → **Cleared** (2 published / 0 pending; 8 rejected).
- Company **259 (AMP Robotics)**: enriched **AMP Delta (1472)** + **AMP Delta Compact (1474)** (rename from Cortex-C) — Available; US; family `amp:delta`; release_year 2024; typed specs from OEM Datasheet 2024 PDFs. Heroes on `research-staging/amp/…`. Rejected Clarity/Insight/SmartTons/Vision/Jet/MicroJet/Cortex/AMP ONE. Soft: Compact weight range left blank; conveyor speed not typed as robot speed. CDN HTTP 200 ×2; **must_clear_pass** ×2. Script: `discover_amp_robots.py`. → Approve tonight (2 pending).
- **`purpose == description` — ROOT CAUSE FOUND + prevented at three layers** (stakeholder: company 50 purposes duplicate their descriptions; make discovery place them correctly and have enrichment check so it cannot recur). **Root cause**: `robot_auto_research.py` derived purpose from the description — `purpose = description.split(".")[0][:200]` (two sites: the enrich path and the post-brief-generation path; a code comment even stated "purpose is only ever derived from the description"). That yields a *first_sentence* copy normally and an **exact** copy whenever the description is one sentence under 200 chars. **Scale** (12 sampled companies, `_purpose_scale.py`): **193 robots** — Kawasaki **56/56**, Estun **92/264**, AeroVironment **25/34**, IIT **9/11**, Unitree 6, Jaten 5; FANUC/KUKA/Noblelift/JAKA/Comau/Ecovacs clean. **Fixes**: (1) *Discovery* — `_CLASSIFY_SYSTEM` now also returns a short task-phrase `purpose` (≤120 chars, no brand, no period); `_classify_robot` sanitizes it; new deterministic `_purpose_from_taxonomy(use_keys, sub_category)` fallback; both derivation sites replaced so purpose is **never** sliced from the description, and a stored duplicate is dropped so it regenerates rather than being carried forward. (2) *Staging gate* — `validate_staging.purpose_duplicates_description()` returns the kind (`exact`/`first_sentence`/`substring`/`near`) and `validate_robot` now **errors** on it, blocking new dupes at import. (3) *Server* — same helper in `robots/quality.py` + new **`purpose_duplicates_description`** WARN flag ("Purpose repeats description") so stored rows surface as a chip in the content queue. Short task words that legitimately occur in the description (e.g. `"Welding"`) are exempt (`PURPOSE_DUP_MIN_CHARS=25`) — verified no false positives. **Tests**: 7 new in `robots/tests/test_quality_flags.py` (exact / first-sentence / verbatim-slice flagged; genuine task + short-word-in-description not flagged; blank → `missing_purpose` not duplicate) — **18 quality tests green**. **Data**: new `fix_purpose_dupes.py` (curated, live rows need `--include-published`) fixed **IIT 9/9 → 0 remaining**; new `regen_purposes.py` re-runs the *fixed* logic (classifier → taxonomy → blank, dupe-guarded at every step) to regenerate the backlog per stakeholder choice — **Kawasaki 56, Estun 92, AeroVironment 25, Unitree 6, Jaten 5 = 184 regenerated, 0 failed, 0 blanked**; re-scan of all 12 sampled companies now reports **193 → 0 dupes**. **DB-WIDE TRUTH (`scan_purpose_dupes.py`, full coverage 4,476 robots / 0 failed pages): 453 duplicates still remain** — the 12-company sample was only ~30% of the real backlog. Split: pending_review 309, rejected 74, published 70; kinds exact 354 / substring 64 / first_sentence 30 / near 5. Worst: SIASUN 64, ACY 47, Yamaha 39, Geek+ 32, Epson 25, DAIHEN 24, Hyundai 23, Huayan 17, AgileX 17, Realman 13, 365Robot 12, iRobot 12, Mujin 11, Stäubli 10, DENSO 10 + long tail. **Scanner trap (fixed):** the first version *broke out* of pagination on a prod 502 and reported "published: scanned 100" as if complete — a partial scan that looked total (real count 1,365). It now skips the bad page, continues, tracks `pages_failed`, and prints `*** PARTIAL ***` / `COVERAGE INCOMPLETE` so a truncated sweep can never be mistaken for a full one. **PUBLISHED FIXED FIRST** (stakeholder: "they are already shown in public, we can't have wrong data shown to the public"): all **70 published** dupes regenerated via `regen_purposes.py --ids-file … --status published --apply` — DAIHEN 24, Epson 23, Realman 13, 365Robot 5, Saab Seaeye 2, Huayan/Richtech/Intuitive 1 each. Old values snapshotted to `staging/reports/purpose-published-backup.json` (70/70) before the write, so it is reversible. Result **70/70 updated, 0 failed, 0 blanked**; full re-scan of published = **1,371 robots / 0 failed pages / 0 duplicates — the public catalog is clean**. Added `--status` (naming `published` IS the live-edit opt-in, so it does not also need `--include-published`) and `--ids-file` (dupes span many companies; the scan reports ids, not company ids) to `regen_purposes.py`. **Still open: 383 non-public** (pending_review 309, rejected 74) — visible as `purpose_duplicates_description` chips in the queue. Skill docs updated: `robot-research-agent/reference.md` gains a **"description vs purpose — DO NOT duplicate"** table; `content-queue-robot-backfill/SKILL.md` gains hard rule **0z**. Lesson: **blank beats duplicated** — `missing_purpose` is a warning, a purpose that parrots the description is misinformation.
- Company **1424 (SIASUN)** CDN heroes: re-hosted **82/82** pending primaries from `en.siasun.com` (56 Chinese-filename URLs) onto `research-staging/siasun/…` via `fix_siasun_cdn_heroes.py` so approve auto-copy-media no longer fails.
- Company **1424 (SIASUN)** purpose: replaced description-copy purposes on **82** pending with short OEM task phrases via `fix_siasun_purpose.py` (0 remaining `purpose_duplicates_description`). Stopped `enrich_siasun_soft` / `fix_siasun_robots` from writing `purpose = description`.
- Company **36 (Figure AI)** stakeholder approved **Figure 03** → **Cleared** (5 published / 0 pending; +1 Figure 03).
- Company **36 (Figure AI)**: enriched **Figure 03 (2502)** — Available; US; Humanoid; family `figure:figure`; release_year 2025 (Introducing Figure 03, 2025-10-09); typed 61 kg / 1727 mm / 20 kg payload / 4.32 km/h / 300 min runtime from OEM `figure.ai/figure`. Replaced wrong **F.02**-labeled CDN hero with OEM **F.03** chest-labeled studio still (`research-staging/figure/…`). Soft: no public MSRP. CDN HTTP 200; **must_clear_pass**. Script: `discover_figure_robots.py`. → Approve tonight.
- Company **266 (Carbon Robotics)** stakeholder approved LaserWeeder G2 200/300/400/600/1200 → **Cleared** (7 published / 0 pending).
- Company **189 (FANUC)** stakeholder approved all remaining except imageless **M-810iA/45 (4117)** → **1 pending**.
- Company **189 (FANUC)** photo holds (4 left after bulk approve): **CRX-30iA/L (4119)** replaced CRX-10iA/L-labeled duo with OEM inverted CRX-30iA still; **CRX-20iA/L (1754)** OEM beauty primary (PTS overlay demoted); **M-800iA/60W (4115)** OEM still arm-labeled M-800iA/60W; **M-810iA/45 (4117)** still imageless — live series is M-810/190+270 only. Script: `_fanuc_photo_hold_apply.py`.
- Company **189 (FANUC)** purpose: replaced junk single-line fluff on **111/111** with OEM applications (newline-separated) via `fix_fanuc_purpose.py` (EU Perfect-Fit scrape + Food/Clean/Wash/Paint variant lines). Skill rule **4b** added.
- Company **266 (Carbon Robotics)**: enriched **LaserWeeder G2 200/300/400/600/1200** (2699–2703) — Available; US; Agricultural-Robots; family `carbon:laserweeder-g2`; release_year 2025 (BusinessWire debut 2025-02-10); typed weight + W×D×H from each OEM Unit Specs PDP; scrubbed Humanoid/Drone tags; EN features + translation-sync. Soft: coverage ac/hr is not vehicle `speed` (left blank); no public MSRP. CDN HTTP 200 ×5 unique; **must_clear_pass** ×5. Script: `discover_carbon_robots.py`. → Approve tonight.
- Company **350 (Stryker)** stakeholder approved Mako 4 + Total Knee/Hip/Partial Knee → **Cleared** (5 published / 0 pending).
- Company **350 (Stryker)**: enriched **Mako 4 (4308)**, **Total Knee (2385)**, **Total Hip (2386)**, **Partial Knee (3288)** — Available; US; surgery taxonomy; OEM US PDPs + Mako 4 brochure. Replaced shared identical 12MB CDN hero across apps with distinct `research-staging/stryker/…` crops/family still. Soft: cart typed specs from OEM tech guide Table 1 (394 kg / 610×889×1423 mm / 6 DOF). Left published **MAKO SmartRobotics (225)** CJK alone. CDN HTTP 200 ×4 pending; **must_clear_pass** ×4. Script: `discover_stryker_robots.py`. → Approve tonight.
- Company **284 (Anduril Industries)** stakeholder approved Fury/Ghost/Altius/Barracuda/Dive-XL → **Cleared** (5 published / 0 pending; Lattice+Menace+Barracuda-100/250 rejected).
- Company **284 (Anduril Industries)**: enriched **Fury (1607)**, **Ghost (376)**, **Altius (377)**, **Barracuda (1605)**, **Dive-XL (1609)** — Available; US; OEM product/news heroes on `research-staging/anduril/…` (replaced shared 2.7 KB placeholder CDN hashes). Rejected **Lattice Mesh (1606)** software, **Menace-X (1608)** C4 platform, **Barracuda-100/250 (1603/1604)** family shells without distinct heroes. Ghost payload 11.3 kg (25 lb OEM). Soft: no public typed dims on most PDPs. CDN HTTP 200 ×5; **must_clear_pass** ×5. Script: `discover_anduril_robots.py`. → Approve tonight.
- Company **29 (Diligent Robotics)** stakeholder approved Moxi + Moxi 2.0 → **Cleared** (2 published / 0 pending).
- Company **1402 (DAIHEN)** stakeholder approved all **24** → **Cleared** (0 pending).
- Company **189 (FANUC)** family metadata: filled `family_key`/`family_name`/`family_url` (+ variant/scope) on **111/111** via `fix_fanuc_family.py` (23 series keys, e.g. `fanuc:m-710`, `fanuc:lr-mate`, `fanuc:crx`). Hard rule added to enrich/discover skills.
- Company **189 (FANUC)** must-clear fix: patched **JP** `manufacturer_countries` on all **111** pending; expanded feat&lt;40; set uses on M-2000iA/2300 (1750); Available; IMAGE TO-DO notes on **M-800iA/60W (4115)** + **M-810iA/45 (4117)** (no OEM still). **109** `must_clear_pass` / **2** hold. Also fixed `moderate_robots.get_company` path (`companies/{id}/`). Script: `fix_fanuc_must_clear.py`.
- Company **29 (Diligent Robotics)**: enriched **Moxi (58)** + **Moxi 2.0 (412)** — Available; US; release years 2020 / 2025; OEM FAQ + Moxi 2.0 launch blog; fixed 412 URL from Squarespace image path; staged distinct heroes on `research-staging/diligent/…`; forced EN via `translation-sync` (zh overlay was masking features → false `no_features`). Soft: no public OEM typed dims/weight/speed. CDN HTTP 200 ×2; **must_clear_pass** ×2. Script: `discover_diligent_robots.py`. → Approve tonight.
- Company **1402 (DAIHEN)** family-7 banner fix: replaced remaining **7** To-Review banner primaries with clean stills → `research-staging/daihen/{id}-*-hero.jpg`; demoted family `mv_*` banners to gallery; cleared `s3_image`. Sources: MAP OTC PDF (V80/V100/V130 labeled); ATC Baltic dealer still (V350); OTC co.id labeled PNG (V400L); OTC V600/700 PNG (V600) + EN catalog heavy embed (V700, distinct hash). Rejected mislabeled OTC/Europe “V350” assets (arm shows V210). Script: `_daihen_family7_apply.py`. CDN HTTP 200 ×7 unique hashes.
- Company **315 (Flexiv)** stakeholder approved Rizon 4/10 (+F/T) + Moonlight → **Cleared** (5 published / 0 pending; Grav EOAT rejected).
- Company **315 (Flexiv)**: enriched **RIZON 4 (2863)**, **Rizon 4 + F/T (3644)**, **RIZON 10 (3645)**, **Rizon 10 + F/T (4587)**, **Moonlight (5165)** — Available; CN manufacturer; OEM parameter JSON typed payload/reach/weight/DOF; Rizon release_year 2019. Replaced Franka-like `reliable.png` heroes with ProductParameter / news stills on `research-staging/flexiv/…`. Rejected **Grav (4588)** + **Grav Enhanced (4589)** as EOAT grippers. CDN HTTP 200 ×5; **must_clear_pass** ×5. Soft: Moonlight no payload column (OEM). Script: `discover_flexiv_robots.py`. → Approve tonight.
- Company **1402 (DAIHEN)** videos: scraped OEM [movie library](https://www.daihen-robot.com/en/library/) — model-tagged YouTube + hosted MP4s (e.g. FD-VC4 `rtj2024/movie04.mp4`); **23/24** pending got ≥1 video; Flat Panel Transfer still 0. Models without a named clip got OTC DAIHEN FD-series programming tutorial as fallback.
- Company **1402 (DAIHEN)** image pass: replaced text-banner primaries with right-side product crops for **16** single-model SKUs → `research-staging/daihen/*-hero-crop.jpg`; full `mv_*` banners demoted to gallery; cleared stale `s3_image` so API shows crops. **7** family-lineup SKUs (V80/V100/V130 + V350/V400L/V600/V700) still banner primaries — title overlays baked across robots, no clean OEM still found. Flat Panel (3468) already clean. Gallery PDFs/`*_img0N` are diagrams — not heroes.
- Company **1402 (DAIHEN)**: discover/enrich complete — rejected **3** legacy Almega AX (RoboDK/dealer URLs); created **FD-V25 (5550)**, **FD-V25L (5551)**, **FD-VC4 (5552)**; enriched remaining FD-* + Flat Panel Transfer from `daihen-robot.com` / `daihen.co.jp`. Company website + `country_id=JP`. Specs from OTC DAIHEN USA + JP wrist tables. **24 pending** — 0 no_image / 0 no_features / 0 short_desc / 0 no_avail / 0 no_specs. copy-media HTTP 403 (external heroes HTTP 200). Script: `discover_daihen_robots.py`. → Approve tonight.
- Company **311 (Realbotix)** stakeholder approved B/M/F → **Cleared** (3 published / 0 pending; Aria already published). Release year 2025 from CES unveil; Available.
- Company **311 (Realbotix)**: enriched **B-Series (4053)**, **M-Series (4055)**, **F-Series (4054)** — Available; DOF 17/39/44; F runtime 240 min min; replaced wrong B full-body hero with bust; F CGI thumb → full-body. CDN OK; **must_clear_pass** ×3. Aria already published (skipped). Script: `discover_realbotix_robots.py`.
- Company **271 (Iron Ox)** stakeholder approved Grover → **Cleared** (1 published / 0 pending).
- Company **271 (Iron Ox)**: enriched **Grover (2979)** Discontinued — company defunct (ironox.com park lander); replaced therobotshq URL with PR Newswire launch; payload 454 kg from official PR (>1000 lb modules); US + agriculture; 3 videos. CDN OK; **must_clear_pass**. Soft no dims. Script: `discover_ironox_robots.py`.
- Company **274 (Picnic Works)** stakeholder approved Pizza Station → **Cleared** (1 published / 0 pending).
- Company **274 (Picnic Works)**: enriched **Picnic Pizza Station (4052)** as Leonardo/Leo config — Available; typed W/H/D from OEM fact sheet; Mikey dims in features; cooking use; 3 videos. CDN OK; **must_clear_pass**. Script: `discover_picnic_robots.py`.
- Company **268 (Harvest CROO Robotics)** stakeholder approved B8 Harvester → **Cleared** (1 published / 0 pending).
- Company **268 (Harvest CROO Robotics)**: enriched **B8 Harvester (4048)** — Available; renamed from generic Harvester; replaced CV-overlay hero with aerial field chassis + picking-modules gallery; features from OEM technology/services/press; 1 video. CDN OK; **must_clear_pass**. Soft `no_specs` (no public dims/speed). Script: `discover_harvestcroo_robots.py`.
- Company **265 (Monarch Tractor)** stakeholder approved MK-V + Vineyard → **Cleared** (2 published / 0 pending).
- Company **265 (Monarch Tractor)**: enriched **MK-V (3875)** + **MK-V Vineyard (3881)** — Available; OEM Spec Sheet typed dims/weight/hitch/runtime/charge; US + agriculture taxonomy; 3 videos each. CDN OK; **must_clear_pass** ×2. Vineyard shares hardware with MK-V (kept as distinct OEM landing). Script: `discover_monarch_robots.py`.
- Company **263 (FarmWise)** stakeholder approved Vulcan + Titan FT-35 → **Cleared** (2 published / 0 pending).
- Company **263 (FarmWise)**: enriched **Vulcan (2762)** Available (OEM flyer speed 6.44 km/h) + **Titan FT-35 (2763)** Discontinued; replaced Titan Carbon-like wrong hero with FarmWise field photo (`research-staging/farmwise/…`); rejected CJK dupes **459** / **456**. CDN OK; **must_clear_pass** ×2. Soft: Titan `no_specs` (no citeable OEM numbers). Script: `discover_farmwise_robots.py`.
- Company **257 (Covariant)**: rejected **RFM-1 (2710)** — foundation model / AI stack, not a robot. Queue empty → Cleared.
- Company **261 (Glacier Robotics)** stakeholder approved Glacier Robot + Fiber QC → **Cleared** (2 published / 0 pending).
- Company **208 (Vecna Robotics)** stakeholder approved AFL/ATG/CPJ → **Cleared** (3 published / 0 pending; CaseFlow + Agile Robots umbrella rejected).
- Company **1424 (SIASUN)** soft-gap enrich: cleared **short_description** (was `"SIASUN"` stubs / &lt;100 chars) and **missing_availability** (30 null → Available). Filled typed specs from live EN PDPs + OEM name encodings (GCR/SA/Nt/P-T). Cleared bad Roller 15 kg misread (family range 15 kg–15 t). Rejected **15** residual SR-SC/SR-MR EN-404 stubs. **82 pending** — soft leftover **2 no_specs** (FP85-50, Cantilever Shaft — parameter tables empty in HTML). Script: `enrich_siasun_soft.py`.
- Company **261 (Glacier Robotics)**: enriched **Glacier Robot (4957)** + **Fiber QC (4958)** from endwaste.io OEM (glacier.ai domain for sale → company website patched). Distinct heroes; Available; sorting taxonomy. CDN OK; **must_clear_pass** ×2. Script: `discover_glacier_robots.py`.
- Company **208 (Vecna Robotics)**: enriched **AFL (4519)**, **ATG (4520)**, **CPJ (4521)** with OEM VecnaFleet renders + cited specs; rejected **CaseFlow (4518)** as orchestration solution. CDN OK ×3; **must_clear_pass** ×3. Script: `discover_vecna_robots.py`.
- Company **208 (Vecna Robotics)**: rejected published umbrella **Vecna Agile Robots (271)** — identical ATG hero hash as **4520**; CJK junk / homepage shell. Keep ATG.
- Company **256 (Berkshire Grey)** stakeholder approved Core/Scoop/Stride/Dispatch → **Cleared** (4 published / 0 pending; Dispatch dupe 4044 rejected).
- Company **773 (Kawasaki Robotics)** stakeholder approved all To Review → **Cleared** (56 published / 0 pending).
- Company **1424 (SIASUN)**: discover/enrich complete — rejected **49** off-catalog EN-404 stubs (SR80C, SR-SC/MR, SCR*, etc.); created **23** live EN catalog robots (7 SA SCARA, 3 GCR cobots, 13 automotive assembly mobiles); fixed features on 6 heavy-duty/G-series mobiles. **97 pending / 97 must_clear_pass**. copy-media returned HTTP 500 — heroes remain verified hotlinked `en.siasun.com` (97/97 HTTP 200). Script: `discover_siasun_robots.py`. → Big fleets (bulk approve).
- Company **256 (Berkshire Grey)**: enriched **Core (4043)**, **Scoop (4045)**, **Stride (4046)**, **Dispatch (2694)** from live OEM PDPs + product sheets; rejected Dispatch dupe **4044** (logo/black-bar hero). Replaced CAD/wrong heroes; Stride hero cropped from OEM sheet → prod CDN `research-staging/berkshiregrey/…` (dev bucket 403). CDN OK; **must_clear_pass** ×4. Script: `discover_berkshiregrey_robots.py`.
- Company **199 (Nuro)**: created **R2 (5526)** Discontinued from OEM spec PDF; rejected Lucid×Uber mashup (**3764**); R3 deferred (no verified hero). CDN OK; **must_clear_pass**. Script: `discover_nuro_robots.py`. Marked done → Approve tonight.
- Company **1308 (UT Austin Robot Perception Lab)**: rejected **Harmon (605)** — CoRL 2024 motion-generation method/paper, not a robot (demos on Fourier GR1). Catalog gap: **0** RPL OEM SKUs (software/algorithms only). Skipped DRACO 3 (Apptronik → HCRL). Set company `country_id=US`. Queue **0 pending**. Script: `discover_utaustin_rpl_robots.py`.

## 2026-07-19

- Companies **285 (Shield AI)** + **190 (ABB Robotics)** stakeholder approved → **Cleared** (Shield 2 published today; ABB **141** published today / 0 pending). Running published-today total → **699**.
- Company **137 (Skydio)** deep discover: created **X10 (5524)** + **X10D (5525)** from OEM PDPs/tech-specs; enriched **X2 (170)** EN + OEM X2E datasheet (replaced wrong Nova-style CDN hero); rejected **R1 (344)** off-catalog + contaminated media; skipped Dock/R10. zh translation-sync ×3; CDN OK; **must_clear_pass** ×3. Script: `discover_skydio_robots.py`. Marked done → Approve tonight.
- Company **258 (RightHand Robotics)** stakeholder approved RightPick 4 → **Cleared**.
- Company **258 (RightHand Robotics)** enrich: renamed **RightPick → RightPick 4 (3808)**; cleared nav-scrape features; aerial→stationary; US country + warehouse/arm taxonomy; OEM `files.svdcdn.com` RP4 hero (transforms CDN 401 without signed params); item payload 3 kg via PATCH. CDN primary OK; moderate **must_clear_pass**. Soft: gallery attachments empty (primary only). Script: `discover_righthand_robots.py`. Marked processed + queue-done → Approve tonight.
- Company **211 (Knightscope)** stakeholder published K5/K7 → **Cleared** (2 published / 0 pending; K1 rejected).
- Company **771 (JAKA Robotics)** stakeholder approved all To Review → **Cleared** (13 published / 0 pending).
- Daily queue report (SGT): **published today = 699** (Approve = Publish; prior ~198 was wrong — missed KUKA + morning clears; +ABB 141 + Shield 2). Pending snapshot earlier: **3467** (**1929** ready / **1538** needs enrichment); Robolist still-to-discover **3407**. Recount: `staging/reports/published-today-recount.json`.
- Company **771 (JAKA Robotics)** 12 To Review cobot arms — shared-primary-photo fix + garbage purge (stakeholder: they all share one primary photo, find correct ones; fix warnings). **Defect**: all 12 shared an IDENTICAL 6-image gallery — a generic company **lineup** (the hero), **4 QR codes**, and a wrong-brand **orange-arm** application photo — content-hash confirmed every image on all 12; and all 12 shared 3 videos about OTHER JAKA products (**Kargo AMR** + a **wheeled humanoid**), not cobot arms. **Fix** (`fix_jaka.py` + `fix_jaka_pad.py`): sourced **distinct per-model renders** from jaka.com — 6 Zu renders (Zu3..Zu20), S5/S12, Pro5/12/16, Mini 2 — as heroes (external → force copy-media beats the sticky owned-CDN s3_image); galleries rebuilt from series renders + genuine JAKA **application** photos (auto-assembly/palletizing/welding for the silver-red arms, coffee/education for the white Mini), distributed to avoid re-sharing; **4 QR + lineup + orange-arm dropped**. Videos → genuine JAKA cobot-arm demos (official JAKA Robotics Zu12/Zu30 + Zu-series; Zu5/Zu12 get their own model video). payload_kg from the name (Zu3=3…Pro16=16; Mini 2 left, ambiguous). **Verify (content-hash + visual)**: **12/12 distinct heroes**, 0 QR/lineup remaining, 5 photos each (few_photos cleared), each hero a genuine JAKA cobot matching its model. **NOTE — prod verify_content + prod-DB flag audit could NOT run: the gcloud/kubectl token expired mid-session** (`gke-gcloud-auth-plugin` reauth needed) — verification done via research API + content-hash instead; a `gcloud auth login` is needed to re-enable server-side verify. **Remaining**: release_year + price (JAKA publishes neither). Zu per-model render↔model mapping is by page order (Zu arms are visually near-identical; all are genuine Zu cobots). Lesson: JAKA galleries hide QR codes + a wrong-brand app photo behind a shared lineup hero — content-hash the whole gallery, not just the hero.
- Robolist coverage rematch (same Jul-18 catalog): robot coverage **20.4% → 21.5%** (+46 matched vs baseline; +4 vs mid-day). Focus OEMs Exail / Huayan / Epson in report. Shareable: `scripts/output/robolist_gap/Robolist_Coverage_Progress.html`.
- Company **211 (Knightscope)** enrich: cleared GSX banner junk from features; K5 (3742) + K7 (3743) OEM copy/media; rejected K1 Hemisphere (3741) stationary. CDN OK; moderate **must_clear_pass** ×2. Script: `discover_knightscope_robots.py`. Marked processed + queue-done.
- Company **285 (Shield AI)** greenfield: created **V-BAT (5522)** + **X-BAT (5523)** pending; skipped Hivemind software. CDN **2/2**; moderate **must_clear_pass** ×2. Script: `discover_shield_robots.py`. Marked processed + queue-done.
- Company **168 (Ghost Robotics)** discovery: enriched published Vision 60 (207) EN + OEM specs/media; rejected Vision 60 Q-UGV dupe (4955) + Spirit 40 off-catalog (4956). CDN Vision 60 OK. Script: `discover_ghost_robots.py`. Marked processed + queue-done → Cleared (0 pending).
- Company **252 (Serve Robotics)** discovery: renamed/enriched published **Serve → Serve Gen3 (286)**; created **Serve Gen2 (5521)** pending. Gen3 press heroes + investor Gen2/Gen3 specs; Gen2 hero = Futurride labeled crop. Skipped Moxi (Diligent 29). CDN **2/2**; Gen2 moderate **must_clear_pass**. Script: `discover_serve_robots.py`. Marked processed + queue-done.
- Company **428 (Exail)** UGV photo redo: Cameleon LG (2753), MK3 (2754), Iguana (2755) — stakeholder rejected blurry catalog sprites; replaced with hi-res OEM datasheet studio/outdoor photos (PDF extract → copy-media). CDN OK; moderate **must_clear_pass**. Still pending until approve.
- Company **73 (Mitsubishi Electric)**: also fully cleared (**26** published / 0 pending). Forced **6** missing gallery 1280 PNG/WebP thumbs; MELFA FR/CRH series shared heroes are labeled family catalog art (intentional).
- Company **400 (Epson / Seiko Epson)**: stakeholder approved **51** published (14 rejected remain). Media audit: **0** dead variants, **0** tiny/missing heroes. Shared hashes are within C-series / LS / GX reach+cleanroom families (OEM catalog reuse) — visual QA confirms EPSON-branded 6-axis and SCARA product shots. Moved to Cleared. Note: checklist **1445 Epson Robots** is a separate blocked company.
- Companies **1028 (Noblelift)** + **239 (RobCo)** post-approve media audit: Noblelift **55** / RobCo **15** published (0 pending each). Dead thumbs were PNG JPEG-map `*_w1280.png` 403s — `repair-images` writes `.jpg`/`.webp` and skips a width when WebP already exists. Force-uploaded **20** missing PNG keys (`_force_png_variants_1028_239.py`); recheck **0** dead. Heroes: RobCo all unique (Alfie + named teal-ring arms); Noblelift family shares intentional; AGV near-dupes **2210↔3336**, **2214↔3335**. Soft: **3378** still FE4P brochure panel (product-correct).
- Company **428 (Exail Robotics)** enrich: set FR country on 6 pending; replaced shared UGV family banner (Cameleon LG/MK3/Iguana identical md5) with distinct OEM catalog assets; replaced A6K UlyX mislabel with labeled A6K produit. K-Ster C already correct. CDN **9/9**; moderate **6/6 publish**. Script: `fix_exail_robots.py`. Marked processed + queue-done.
- Company **195 (Rethink Robotics)** discovery wrap: OEM site dead (0 bytes even stealth); catalog = Baxter (5315) + Sawyer (5316) only; URG no longer lists Rethink products. Replaced robotsguide aggregator URLs with Wikipedia; set discontinued. CDN heroes kept. Script: `discover_rethink_robots.py`. Marked processed + queue-done.
- Stakeholder approved Richtech (131), Apptronik (14), Agility Cassie → Cleared in approve checklist.
- Company **109 (Unitree)**: last 3 To Review (**5362 R1-A7**, **5355 Aliengo**, **5353 A2-W**) — false `no_country` (already China FK; filled `manufacturer_countries`); soft no-video/no-price OK; published + `published_at` via DRF `/review/`. Fixed **A2 (5354)** wheeled↔footed contamination → OEM footed studio `e0b39e851afc`; **Go2-W (644)** had same A2 studio → OEM Go2 canyon wheeled `e3faa3edc876` + URL `go2-w/`. Variant repair on 8 robots (missing JPG 960/1280). Family shared heroes (Go1/R1/H1/G1/R1-A5↔A7) intentional. Residual: gallery photos on the last 3 still `pending_review` (need admin Approve or `ADMIN_SESSION_ID` photo publish — browse cards use `s3_image`); R1-A7 gallery still has D-variant hash `605fbd01f28c` pending. Scripts: `fix_unitree_pending3.py`, `fix_unitree_a2_r1a7_media.py`, `_apply_go2w_hero.py`, `_audit_unitree_109.py`.
- Company **1028 (Noblelift)** 55 To Review robots — media-garbage cleanup + product-image match + fixable warnings (stakeholder: fix warnings, no garbage image/video, primary image must match product). **Garbage VIDEOS**: 11 AGV robots carried 3 NON-Noblelift clips (uploaders "Vincent Feng"/Wuhan Donglisheng M&E, "campur gado chanel", "LOGIBRIDGE") → replaced with official **Noblelift Intelligent** AGV videos (`vm1b-_WbSCA` + 2 customer cases; APT15 gets its own `3alOSdsZFjA`). Every other video group verified as a Noblelift regional channel (Middle-East/NA/Germany/Canada/distributors showing genuine product) — kept. **Garbage IMAGES**: (a) 25 robots had a **525-byte broken placeholder** photo; (b) a shared **Australian flag** (sha `1001e2fd5d`, 18 robots) and **NOBLELIFT logo** (sha `471a571ec5`, 12 robots) padded galleries — survived the size filter, only content-hash caught them (same per-robot-CDN-key trick as FANUC); both purged via `fix_noblelift_junk.py`. **Wrong-product HEROES**: 3187 hero was a 525-byte broken PNG; **3378 "Counterbalanced Forklift" hero was a forklift AIR-CONDITIONING accessory banner** (not a forklift); 3379 was a partial cab crop → repointed each to a product-forward Noblelift banner (external → force copy-media to beat the sticky owned-CDN s3_image; first picked the wrong FE4P feature-grid for 3378, swapped to the clean FE4P16-35N render). **Warnings**: payload_kg from the model's rated capacity (name-encoded, OEM-confirmed: PS20=2000kg, RT16=1600kg, APT15=1500kg…) cleared **missing_specs 29→3** (the 3 left are generic category records with no capacity in name); availability=released on OPL10+ES15 (ORM) cleared missing_availability 2→0. **Prod verify_content** on the media-changed set: all clean after fixes — 3378 92, 3379 96, 3187 94, 3186 94 (its 77 image_mismatch was the flag — cleared on purge), AGV robots 87-94. **Remaining (reported, not fixed)**: release_year 55 + price 55 (Noblelift publishes neither) and **few_photos 18** — the direct cost of removing the flag/logo garbage (Noblelift ships only 1-2 images per model; padding with garbage/dupes was declined). Lessons: a size>2KB filter does NOT catch flag/logo junk — always content-hash galleries; heroes/videos verified but gallery junk needs its own pass; the sticky owned-CDN hero only refreshes when images[0] is an external URL copy-media can fetch (noblelift.com is fetchable).
- Company **14 (Apptronik)** enrich: Apollo (37) English features + unveil specs (173 cm / 72.6 kg / 25 kg) + US; Apollo 2 (1634) renamed, Webflow template junk replaced with OEM Apollo 2 page copy. `/apollo` 404. zh overlay cleared via `translation-sync/?force=1`. CDN **2/2**; Apollo moderate **publish**. Script: `discover_apptronik_robots.py`. Marked processed + queue-done.
- Company **131 (Richtech Robotics)** curated discovery: enriched Dex (5277) + ADAM (162); created Titan (5515), Matradee Plus (5516), Scorpion (5517), DUST-E S (5518), DUST-E MX (5519), Pallet Jack/F3300 (5520). Stealth required for richtechrobotics.com. Visual QA: use labeled product OG banners (Titan/MX/Pallet); ban ADAM hashes on sibling pages; Pallet Jack needs `get_rendered`. Scrubbed non-Richtech Titan YouTube (BMW/Porsche Titan 440). CDN **8/8**; moderate **7/7 publish**. Script: `discover_richtech_robots.py`. Marked processed + queue-done.
- Company **1396 (KUKA)** all approved/published (**255**). Image-variant deep audit: **4320** thumb URLs probed; only **KR 6 (62)** had dead primary JPG 640/960/1280 (WebP OK) — force-uploaded; re-verify HTTP 206. Moved to Cleared.
- Company **1396 (KUKA)** approved-set + To Review media/QA: root cause = `DOWNLOAD_HEADERS` AVIF Accept → KUKA content-negotiated stubs. Fixed Accept preference in `robots/utils.py` (deploy needed). Repaired **28** published + **103** pending AVIF heroes via wsrv-forced family PNGs (`fix_kuka_published_avif.py`, `fix_kuka_pending_avif.py`). Tags fixed on 15 pending warn targets (`fix_kuka_pending_warns.py`). Family shared heroes stay intentional.
- Company **9 (Agility Robotics)** discovery: enriched published Digit (11) EN + OEM heroes/CDN; created **Cassie (5514)** with OSU newsroom photos. Skipped Digit V1/v5/Humanoid phantoms. Cassie moderate **publish**. Scripts: `discover_agility_robots.py`, `_attach_cassie_media.py`.
- Company **92 (Sanctuary AI)** greenfield discovery: auto-crawl staged junk (hands / phantom category); curated import **Phoenix (5513)** only — Gen 8 OEM heroes ×3, Gen 6 unveil specs, 3 titled YouTube, Canada + Humanoid. CDN **1/1**; moderate dry-run **publish**. Script: `discover_sanctuary_phoenix.py`. Marked processed + queue-done.
- Company **882 (Realman)** missing named variants: created **ECO63 Standard (5511)** + **ECO63 Six-Axis Force (5512)** as `pending_review` (CDN OK); renamed published **5230** → **RX71 Standard**. Did **not** create ECO62/RX71/RX75 Force — OEM evidence shows they are not separate SKUs. Script: `discover_realman_missing_variants.py`. Report: `staging/reports/realman-missing-variants-create.json`.
- **Moderation workflow B** started: playbook [playbooks/approve-reject-robots.md](playbooks/approve-reject-robots.md), skill `.cursor/skills/robot-moderation-queue/`, script `moderate_robots.py`. Product rule: **Approve = Publish** for robots (content-queue Approve → `published`).
- Company **882 (Realman)** published (all **26**). Variant gap review: missing named **ECO62 Force**, **ECO63 Std/Force**, **RX71 Std/Force**, **RX75 Force**; 6 bare-family near-dupes (RM65/75/RML63/ECO62/65/RX75) candidates to reject/merge into Standard; accessories (gripper/teleop/joints) out of scope; RealBot-01 URL should be `realbot-humanoid.html`. Report: `staging/reports/realman-variant-gap-curated.json`.
- Company **882 (Realman)** gallery expand: `fix_realman_galleries.py` — OEM `/prop/products-images/` multi-angles; Standard/Force/Vision folder separation; hash-dedupe by model_key. **23/26** galleries expanded (17×4+, 6×2–3); chassis + RX71 stay 1-shot. CDN verify **26/26**. Price/year still blank (no public OEM citation). Report: `staging/reports/realman-galleries.json`.
- Company **1413 (Pangolin)** photo audit: 18/18 still 1 photo (OEM PDPs share sibling/footer galleries — fail-closed). Fixed **2195 Niumowang F300** wrong Medical hero → correct F300 list-card thumb (`6ce9fce4cd87`).
- Company **1413 (Pangolin)** post-approve polish: stripped `Pangolin Robotics ` name prefix (product-only names); replaced Xiaoyu hash-titled YouTube with readable Timo clip; dropped hashtag-only Panda short. Hardened `youtube_metadata` title reject (hash/UUID/WeChat/hashtag dumps). Script: `_fix_pangolin_names_videos.py`.
- Approve/publish status checklist: [checklists/approve-publish-status.md](checklists/approve-publish-status.md) — Approve tonight / Big fleets / Partial / Needs cleanup, with Status + Pending columns for team updates. Seeded from `_audit_ready_to_approve.py` (Pangolin country fixed → Approve tonight).
- Company **1413 (Pangolin Robotics)**: `fix_pangolin_robots.py` — list-card thumbs (not contaminated PDP banners). **18** robots unique CDN heroes + features/tags/China/taxonomy; **26** imageless leftovers with `[IMAGE TO-DO]` (duplicates / unlabeled variants). CDN verify 18 ok / 26 deliberate empty. Not marked queue-done (leftovers remain). Prefer `alpha-robot.com.cn` over expired `csjbot.com`.
- Company **1413 QA clear**: `fix_pangolin_qa.py` — company website → `alpha-robot.com.cn` (cleared `url_domain_mismatch`×44); stripped CJK from EN descriptions (`non_english_content`); **rejected 26** duplicate/category shells. Pending **18/18 now 0 ERROR flags** (warns remain: few_photos / no year / no price / no specs / no video). Re-run QA Check in admin to refresh chips.
- Company **1413 WARN fill**: `fix_pangolin_warns.py` — OEM-cited specs on **16/18** (Max variants fail-closed: shared Speedybot PDP dims are Pro-scale); brand-gated YouTube on **12/18**. Residual warns honest: few_photos (1 OEM render/model), no MSRP, no launch year, 6 no clean video, 2 Max missing_specs.
- Robolist coverage progress: rematched same catalog snapshot vs current inventory. Robot coverage **20.4% → 21.4%** (+42 matched). Shareable report: `scripts/output/robolist_gap/Robolist_Coverage_Progress.html`. Baseline archived under `staging/robolist_gap/baseline_2026-07-18/`.
- Hard rule: never put OEM URLs in description/features — `information_source_urls` only (SKILL.md 4a).
- Company **192 (Universal Robots)** videos + AI verify: filled **21** missing model-titled YouTube clips; `verify_content --force` on **32/32** pending. Retargeted redirecting UR5e/UR5/UR10 URLs to OEM tech sheets/PDFs → scores recovered (~99–100). UR18 still no model-titled video found.
- Company **192 (Universal Robots)** imageless chase: found model-named Storyblok/`/media/` heroes despite PDP redirects; `fix_ur_imageless.py` → **5/5** (UR5e/UR10e/UR3/UR5/UR10); CDN **32/32**; marked queue-done.
- Company **192 (Universal Robots)**: `fix_universal_robots.py --apply --copy-media` on **32** pending cobots — Denmark + Collaborative/Industrial taxonomy + OEM payload/reach; Storyblok heroes + copy-media. CDN **27/32** OK. Force-replaced bad keeps (diagram / cobots_hero banner / shared app shot / infographic). **5 IMAGE TO-DO** (UR5e/UR10e/UR3/UR5/UR10 — PDPs redirect; no model-specific render). Not marked queue-done. Deferred **1480 MUKA** (messy PDPs).
- Company 1396 (KUKA) **series hubs**: kept all **15** ex-57 family placeholders as intentional series hubs (`fix_kuka_series_hubs.py`); `[SERIES HUB — keep]` notes + `product_url_scope=family`.
- Company 57→1396 **post-merge**: remapped **15/15** `kuka-robotics:*` family_keys → `kuka:*` (`fix_kuka_post_merge_family_keys.py`). Fleet now **255** robots; 0 exact name dups.
- Company 57 (KUKA Robotics) vs **1396 (KUKA)**: confirmed **same OEM**. Enriched all **15** co.57 robots (`fix_kuka57_robots.py`); recommend merge **57 → 1396** (keep 1396). Family-key collision blocks shared `kuka:` keys until merge.
- Company 1396 (KUKA) **old fleet + prices**: `fix_kuka_old_fleet.py --apply` → **103/103** family/specs/year/video where OEM-grounded. **No public KUKA MSRP** — cleared 68 unsourced estimate `price_min`/`price_max` rows (`fix_kuka_clear_prices_and_leftovers.py`); depth imports left blank. Honest leftover notes for discontinued/gen-1/placeholder models.
- Company 283 (AeroVironment) **4 To Review robots enriched** (stakeholder: fix all errors/warnings except No Price). Per-robot, driven by the real `robots/quality.py` flags computed from **prod base fields** (the research API's zh-CN/zh-TW translation overlay made my first audit misread `country`=None and `description`=Chinese — both false; verify flags via prod DB, not the API). **Pro 5 (1509)** UMV subsea ROV: its s3_image was byte-identical to a VigilantHalo shelter photo (cross-contaminated) + 2 gallery photos were a soldier-w-UAV and a fixed-wing-on-rocks → rebuilt gallery to 4 verified yellow-ROV shots (correct hero), added the genuine VideoRay Pro 5 demo (`UypS8GORaPg`), typed dims 515×330×257 mm + 10 kg, added missing **category** (159 Mobile Robot, via ORM — matches sibling Defender), release_year 2019. **Defender (1510)**: typed dims 711×394×241 mm, release_year 2019, swapped 5 generic-AV-corporate videos for 2 Defender-specific ones (cleared `video_mismatch`). **VigilantHalo (1506)** software C2 platform: set `connectivity` (its only real "spec"), and reworded the auto-generated "VigilantHalo is an aerial robot" description/purpose (cleared `content_contradiction`) — it's a C2 platform, not an aerial robot. **Wasp (519)** = the mini-UAV (photos/videos/2012/Aerial confirm it), but its stored `avinc.com/solution/wasp` now serves an unrelated **space antenna** ("Wide Area Scanning Parabolic") and AeroVironment retired the Wasp AE (all its avinc datasheets 404). Translated its Chinese description/purpose/features to English (base fields — the `non_english_content` ERROR reads base, not the zh overlay), added cited specs (1.3 kg / 760 mm / 1020 mm span), and added a verified Wikimedia Wasp III photo → 4. Per operator decision the source URL was repointed to an authoritative off-domain ref; first tried Airforce-Technology (bot-walls the verifier → `unverifiable` 94) then **Wikipedia** (fetchable) — and made the Wikipedia page's own primary image (the white Wasp III photo) the hero so it matches the source → verify **86 clean**. **Final: prod verify_content = Wasp 86 clean, Pro 5 98 clean, Defender 99 clean, VigilantHalo 89 verified** (one soft residual `video_mismatch` on VigilantHalo's *official* C2 video — inherent to scoring a software product against a robot-oriented verifier). **Deterministic flags remaining: only `missing_price` (excluded per stakeholder) + Wasp `url_domain_mismatch` (the accepted cost of the off-domain authoritative ref).** Note: Wasp + VigilantHalo now have **stale zh-CN/zh-TW translations** (base English changed; translation pipeline should refresh). Lessons: (1) copy-media's 500/HTTP-status is unreliable AND avinc.com bot-blocks the server's hero copy (external avinc hero won't land — the owned-CDN fallback hero is correct though); (2) an owned-CDN hero is sticky (copy-media force skips healthy owned), so to change WHICH owned image is hero you must lead the `images` PATCH with an external URL the server can fetch (Wikimedia works, avinc doesn't).
- Company 1396 (KUKA) **descriptions**: `fix_kuka_descriptions.py --apply` → **131/131** short_desc robots rewritten from OEM payload/reach + feature-table facts only (no invented applications). Lengths ~150–220 chars; 0 still short.
- Company 1396 (KUKA) **URL/video/family**: `fix_kuka_url_video_family.py --apply` → **137/137** family series metadata filled; series-named videos; OEM family URLs (third-party exact pages bot-walled → reverted). Force re-verify completed.
- Company 1396 (KUKA) **availability / year / video**: `fix_kuka_availability_year_video.py --apply` → **137/137** `released` + family `release_year` + ≥1 titled YouTube. Prod `verify_content` batches of 10 → **137/137** confidence (avg 85.7; 130 verified / 7 review).
- Company 1396 (KUKA) **required-field remediation**: standing enrich rule = must clear photo + country + categories/uses (price/videos optional). `remediate_kuka_required_fields.py --apply --copy-media` → **137/137** Germany + Industrial-Robot taxonomy; **24/24** remaining heroes attached (FORTEC / ultra / ultra-PA / LBR iiwa CTAs); CDN **24/24**. Country PATCH must use full name `"Germany"` not `"DE"`.
- Company 189 (FANUC) **QA pass finished — mis-assigned videos + no-image robots closed** (continues the 2026-07-16 URL/video audit). (1) **21 all-software-video robots repaired** via `fix_fanuc_sibling_videos.py --apply`: these carried ONLY CNC/software promos and the DRF serializer can't clear `video_urls` to empty, so instead each inherits a genuine same-series sibling's robot video (PATCHing a NON-empty list replaces the junk cleanly) — **21/21 inherited, 0 failed** (M-10iD→M-10iD clips, M-20iD→M-20iD, M-410→valve-bag-filling, R-2000→spot-welding, M-900→M-900, M-2000iA→M-2000iA/2300, SR→SR-12iA SCARA). Only **P-40iB/10L** left (series `p-40` has no real FANUC video anywhere). Combined with the 23 stripped on 07-16, the software-promo videos are gone. (2) **22 no-image robots sourced** via `_fanuc_noimg.py` (scrape each robot's now-fixed fanucamerica page for a render, `/assets/images/` **and** `/assets/products/<series>/`) + `attach_fanuc_noimg.py --apply` (PATCH `images` external-first → force copy-media). Every hero **visually verified** off two contact sheets — rejected FANUC Academy cards, people-with-cobot training photos, a food-grade *label* graphic, CRX cross-nav, and mobile-cart art. **20/20 heroes CDN-live + owned (content-hashed, not trusting copy-media status)**, galleries 1–2 imgs. **2 have no live page → left blank + documented**: 4115 M-800iA/60W, 4117 M-810iA/45. Within-series hero sharing is the accepted family trade (M-1iA pair, M-2iA pair, M-3iA pair, R-1000iA trio, **R-2000iD quintet** all share one clean series arm — no per-variant render is published). SR-12iA got the SR factory-floor shot (only SR image available). Traps: the two `/assets/products/<series>/` heroes 404 under the `/assets/images/` prefix my scraper assumed — full URLs required; and the R-2000 series page's clean arm is filed as `R-2000iC-165F` (iC, not iD) but is the correct family render.
- Company 109 (Unitree Robotics): discovery import + enrich + QA wrap. Discovery added 25 `pending_review` robots (5349–5373); fleet now 49. Auto pipeline enrich 49/49 + copy-media; CDN verify 49/49. QA remainders fixed via `fix_unitree_robots.py` (English narrative + zh-CN translation-sync force, OEM specs on H1/G1/B2/Go2/Go1). Leftover: GD01 601/660 no PDP (merge candidates). Price/videos optional, left as-is. Marked content-queue done 109. Lesson: research API preferred_language zh-CN overlays translations — force `translation-sync?force=1` after narrative overwrites.
- Company 1396 (KUKA): depth discovery via OEM family tables (`discover_kuka_depth.py`). Triage 137 recommend / 6 review / 63 already in DB. Imported recommend only → **137 created** as `pending_review` (IDs 5374–5510); 0 errors. Enrich QA (`enrich_kuka_depth.py --apply --copy-media`): OEM payload/reach **137/137**, external Serper cites **136/137** (evidence in `staging/evidence/kuka-depth-20260718/`), family heroes **113/137** then remediations filled **24** more → **137/137** with photo + country + categories/uses. Years/videos left blank where uncited.

## 2026-07-16

- Company 189 (FANUC) **URL + video QA pass** (stakeholder-reported: no images, dead source URLs, mis-assigned videos e.g. DR-6iB carrying M-6iB clips). Audit (`fanuc-qa.json`): 22 no-image, **50 dead source URLs**, 59 robots with FANUC **CNC/software promo videos** (not robot demos: "What you can do with the servo model" ×46, "CNC GUIDE 2" ×37, "CNC Reflection Studio" ×27+21, "100x simulation" ×38). Fixes: (1) `fix_fanuc_urls.py --apply` remapped **47/50 dead URLs** to live fanucamerica pages (series slugs verified by probe: `m-1ia`/`m-2ia`/`m-3ia`/`r-1000ia`/`r-2000ic`/`lr-mate`/`crx`; individual `/products/robot/<model>` for M-800iA/M-950iA/M-1000iA/LR-10iA/ER-4iA). **Match on the M-<num> token BEFORE the `i`, not raw prefix** — `m-10` was wrongly catching `m-1000ia`. **50→3 dead** (only M-800iA/60W, M-810iA/45, DR-6iB have no live page). (2) `fix_fanuc_videos.py --apply` stripped the software promos: **23 robots cleaned** (kept their genuine robot video); **21 robots whose videos were ALL software can't be cleared** — the DRF serializer ignores an empty `video_urls` (can't clear to zero), so they're reported for a manual/server clear. (3) DR-6iB (the reported example): replaced its 3 wrong M-6iB videos with the correct DR delta demo (`ziIvXVLlVI0`, "DR-3iB/6 STAINLESS delta") and pointed its URL at the DR series page. Open: 22 no-image (mostly the source-less SR/heavy models) and the 21 all-software-video robots (need a video-clear path).
- Company 189 (FANUC) **junk-image purge COMPLETE + server-side copy-media fix**. Follow-up to the entry below: the 63 blocked junk heroes are now down to **2** (only SR-3iA/SR-6iA SCARAs — FANUC's SCARA page has no filename-matchable SR photo). **Root fix was server-side** (`robots/background.py`, `robots/utils.py`, `robots/admin.py`): (1) `_copy_media_worker` now takes `force=` and re-copies an EXTERNAL hero even when `s3_image` is already set — previously it only ran `if image and not robot.s3_image`, so a robot whose `s3_image` pointed at a bad object could never be repaired; (2) a healthy owned-CDN url (new `owned_media_url_is_healthy` HEAD/GET check) is now `skipped`, not a failure, so copy-media stops answering HTTP 500 on good robots; (3) the endpoint accepts `?force=1` and returns a `{copied,skipped,failed}` report. Tested: **12 new + 33 existing copy-media tests green** (DB was reachable only over IPv6 `DB_HOST=::1` — the host's IPv4 forward to the `robot_ai_geek_db` container dropped during a session interruption; Postgres itself was fine). Client side: `fix_fanuc_photos.py` rebuilds each gallery **external-photo-first** (so `Robot.image`=external) then calls copy-media with **`?force=1`** → refreshes `s3_image` from the real photo. Repaired **57 + 5** (the last 5 = CRX/M-1iA, whose photos my recon's strict filename filter had missed — real slugs are `/series/crx` + `/series/m-1ia`, photos like `crx-handling-metal-block.jpg` / `m-1ia-pick-erasers.jpg`, visually verified; rejected a "CRX eLearnings" marketing card). Final: **JUNK 63→2, 0 broken, distinct images 83→102, worst reuse now a real M-710 photo ×32**. `fix_fanuc_heroes.py` deleted (its bulk-import `replace_media` path hits a different server recopy fn and never worked — superseded).
- Company 189 (FANUC) **junk-image purge — galleries fixed, heroes blocked**. Audit found the shared-hero defect at scale: 111 robots shared only **83 distinct images**, the most-reused being corporate marketing art — a capability infographic on **75** robots, "Reliability/Efficiency/Flexibility" triangles on 42+35, FIELD architecture schematics on 42+33, a Japanese **CSR materiality matrix** on 8, a "BOT CON" banner on 5 = **299 junk attachments over 67+ robots**. URL dedupe is blind to it (each junk image sits on its own per-robot CDN key) — **content-hash only**. New `fanuc_recon.py` (fanucamerica.com series pages scrape over plain HTTP; media on cdn.craft.cloud; 43 visually-verified real FANUC photos across 13 series; robot URLs are per-MODEL `m-710ic-20m` so they need longest-prefix mapping to the SERIES key `m-710`). `fix_fanuc_photos.py --apply` → **77/77, galleries verified 0 junk**; distinct images 83→103, worst reuse 75→32 (now a real M-410 palletizer photo). **STILL OPEN: 63 robots keep a junk HERO** (`Robot.image`/`s3_image`) — a platform blocker, not data: DRF PATCH `images` sets `image` but never `s3_image` (which the UI reads); DRF PATCH `image` is silently ignored; bulk-import `replace_media`'s `force_patch_keys={'url','image'}` never fires because `map_to_bulk_import` folds `image` into `images[]` (server sees `row['image'] is None`), and its sync recopy 500s. `copy-media` — the only thing that refreshes `s3_image` — returns **HTTP 500** on these (fail-closes on owned-CDN heroes); it succeeded on robot 4190, whose hero is now a verified real M-20iD photo. Needs a server-side fix (management command, or let `image` survive as its own bulk-import key, or make copy_media skip-not-fail on owned-CDN). Also: **never trust the copy-media status code** — it returned 500 on robot 1738 while the purge and s3 copies actually succeeded, and 500 here while nothing changed; always re-fetch and content-hash.
- Company 1396 (KUKA) **features pass — cracked the family spec table**. Picked as next target by a targeted candidate probe (KUKA 75 incomplete / 103 pending, top of 9 candidates with real OEM sites; deliberately avoided teammate's in-flight 245/32). Audit (with real photo GETs, not `robot_gaps`): **73 feat<40**, 67 <4 photos, 15 no valid photo, 35 no year, 16 no videos. **Key discovery**: KUKA family pages (`/industrial-robots/<family>`) render their per-variant products table **server-side in plain HTTP** — no JS/stealth. Each `js-item list__item` carries `data-name` / `data-load-capacity` / `data-reach` + Construction type / Mounting positions / Protection class / Controller. Naive scrapes missed it because **KUKA labels payload "Total load", not "Payload"** — exactly why the overnight run left "KUKA 54→54". New `kuka_recon.py` scraped 26 family pages → **206 variants** (`staging/reports/kuka-recon.json`). Name matching needs normalization: the DB appends suffixes KUKA's table omits (" IONTEC", " ultra", " nano") → **61 → 72/103** matches. `fix_kuka_features.py --apply` → **72/72 updated, 0 failed**; features ~27-67 → ~226-276 chars of OEM-cited spec. **Deliberately APPENDED, never force-overwrote**: the `feat<40` rows were NOT junk (unlike EFORT) — they held short but valid, model-distinct application keywords ("Heavy assembly, force-sensitive tasks, human-robot collaboration") alongside good descriptions; `<40 chars` is a length heuristic, not a junk detector. payload/reach/IP still have no Robot columns, so features is their only home. Also fixed `triage_content_queue.iter_pending_robots` — it had **no retry** and died on a single 502 at page 2 of 3952 (now 6× backoff, mirroring `list_robots_for_company`). Second pass added a **tiered-trust matcher** → **82/103** total (0 failed): family-word strips (" IONTEC"/" ultra"/" nano"/" Delta") are trusted (can't change which robot is meant), while generation hops are **payload-guarded** (`KR 340 R3200 PA`→`KR 340 R3200-2 PA` only because Total load 340==340; `KR 120 R3200-1 PA`→`KR 120 R3200 PA`; `KR 8 R1440 arc HW-2`→`KR 8 R1440-2 arc HW`). **The guard caught a real trap**: `KR 3 D1200 Delta`→`KR 3 D1200` lists **Total load 6 kg** for a 3 kg robot — KUKA's "Total load" is payload **+ supplementary load**, so the name number ≠ total load on some families; guarding family-strips would have wrongly rejected a correct match (hence the two tiers). **Gen-1 arms are deliberately NOT hopped** (`KR 120 R2700` ≠ `KR 120 R2700-2` — different generation/controller KR C4 vs C5). Also: `kr-scara` and `lbr-med` URLs stored on the robots are **dead** (both serve a generic "Industrial robots from KUKA" landing, 0 variants) — the live pages are `kr-scara-robot` (18 variants) and, per KUKA's own embedded JSON, LBR Med under `/industries/robots-used-in-medicine/`. Open: **21 unmatched** (4 gen-1 arms by design; 7 AMR KMP/KMR/omniMove — marketing pages, no spec table, only tokens like "KMP 600P diffDrive"; `KR SCARA`/`KR 4 AGILUS` are family names not variants; `KR 6 R1440-2 nano` doesn't exist in KUKA's lineup; 2 LBR Med) and **photos untouched** (15 have NO image URL at all — LBR Med ×2, nano ×4, "K" ×3, delta, Z-series SCARA ×5; 67 <4). KUKA renders live at `/-/media/kuka-corporate/images/products/robots/<family>/…` but are **family-level, not per-variant**. Per stakeholder ("attach family renders to the 15"), `fix_kuka_photos.py --apply` → **15/15 image=updated, copy-media 15/15, `verify_cdn_images.py` 15/15 HTTP 200** (all 15 had `image=""`, so nothing was overwritten; same-family variants now share one render — an accepted trade vs. no photo). Renders used (each **downloaded + visually verified**): KR SCARA white 4-axis render → the 5 Z-series; `cta-images/kr-cybertech-nano.png` (orange arm on black) → the 4 nano; `cta-images/kr-quantec.png` → the 3 "K" shelf-mounted variants; `kr-delta/delta-secondary-packaging.jpg` → KR 3 D1200; `industries/healthcare/lbr-med/lbr-med-feature-cell.jpg` → the 2 LBR Med (found only after locating the **real** LBR Med page at `/en-us/industries/health-care/kuka-medical-robotics/lbr-med` — the stored `/industrial-robots/lbr-med` URL is dead). **Visual QA rejected**: KUKA's `-10 %` text-overlay marketing graphic, 3-arm family lineups, a steel-truss photo, a Sunrise.OS laptop screenshot, a people-at-a-desk shot, and the kr-scara page's **sibling cross-nav art** (an orange 6-axis arm / a delta / an AGILUS embedded on the SCARA page — the Comau cross-nav trap).
- Company 245 (Comau) **text/specs/tags sweep — queue cleared**: a bad scrape had stored the site's **cookie-consent banner** as `features` on **38/48** pending robots (identical 774-char blob, `IABV2SETTINGS`), with `Humanoid`/`Mobile Robot`/`Logistics` auto-tagged onto industrial arms. Neither bulk-import mode is usable here — `patch` won't displace non-blank junk and `force_overwrite` blanks unsent fields (would wipe the media we'd just repaired) — so `fix_comau_text.py` writes via **targeted `client._patch('robots/robots/<id>/')`** (`RobotSerializer` exposes `features` text + **`tags` writable ListField**, plus `dof`/`weight_kg`). Verified on one robot (photos/hero intact) before the fleet. **Result: 48/48 patched, 0 failed** — junk features **0**, off-family tags **0**, `release_year` set **0** (all NULL, no per-model launch citation), `dof` 6×39/4×5/5×1, `weight_kg` 46/48. Tags are now **per-family** (Comau is not an all-arm maker): NJ/NJ4/N/S = 6-axis arms; **Rebel-S6 = 4-axis SCARA** (name lies — "S6" is the 6 kg payload); MyCo + Racer-5-0.80 COBOT = collaborative; PAL-180/260 4-axis, PAL-470 5-axis; **1889 is a genuine AMR** (the only row where Mobile/Logistics tags are right). Specs applied from `comau-recon-all.json` with the European-separator fix (`1.300 mm`=1300 vs `2,5 m/s`=2.5), per-field unit binding (blocks `350W`→weight), and name-convention cross-check dropping any flagged value; `payload_kg`/`reach_mm` remain non-columns → kept in features+notes. The 6 intentionally-imageless rows (1852/1854/1855/1858/1862/1864) got text/tags/specs with **no banner or map re-added**. Zero published robots touched (400–405). Preview: `staging/reports/comau-text-preview.json`.
- Company 245 (Comau) **media defect repair**: an earlier run had set Comau's corporate world-map graphic (`/wp-content/uploads/2024/09/global-presence-2.webp`, md5 `f9f947f91fd616172a68268be1ae7758` — the "Global Spirit, Local Presence" footer art, **lazy-loaded on every Comau PDP**) as the **primary hero on 32 pending robots**, byte-identical, plus rife gallery duplication. Our CDN re-serves it per robot (`robot-<id>-<slug>.webp`) so **only byte hashing detects it** — and it serves `Content-Type: application/octet-stream`, which silently defeats content-type guards (this hid it from my own first audit pass). New reusable tooling: `comau_image_audit.py` (hash every candidate + current primary + `photos[]`, cluster, flag banned), `comau_hero_plan.py` (ownership by filename **payload+reach digits** — not family prefix — - dedupe, fail-closed, `HERO_OVERRIDE` for visual calls), `comau_contact_sheet.py` (tiles N images into ONE labelled PNG; reviewed 141 galleries + 36 heroes in 2 Reads), `fix_comau_media.py` (media-only `patch+replace_media` + permanent `assert_media_safe` pre-apply gate). **Result: 25/32 map-heroes replaced** with hash-audited, visually verified per-model renders (zero drawings found in the selected pool); company-wide **duplicate primaries (excl. map) = 0**; copy-media OK. **7 robots still carry the map and are BLOCKED** (1852 S-13; 1854/1855/1858/1862/1864 MyCo; 1857 Racer-5-0.80 COBOT): they genuinely have **no per-model render on comau.com** (MyCo PDPs publish only a shared family banner; S-13's page serves only S-18 assets), and **there is no API path to clear a hero** — bulk-import patch never blanks a field and `PATCH robots/robots/<id>/ {"image":""}` → 400. Needs a server-side clear, admin-UI action, or a licensed third-party image. Zero published robots touched (400–405). Also documented: Comau spec tables use **two dialects** (unit-in-label vs unit-in-value) and **European separators** (`1.300 mm`=1300, `2,5 m/s`=2.5) — a blanket comma→dot replace corrupts reach (S-13 1.960 mm → 1.96); units must be bound per field or `350W`/`35 Nm` parse as weight. **Rebel-S6 is a 4-axis SCARA**, PAL-180/260 are 4-axis, PAL-470 5-axis — type from the table, names lie. New hard rules 9a/9b/9c added to the skill (no drawing as hero; dedupe by content hash; fail closed on media).
- Company 245 (Comau): greenfield TEST batch. Backfilled **6 pending_review** NJ-series industrial arms (NJ-100-3.2 PRESS `4165`, NJ-130-3.7 PRESS `4166`, NJ-165-3.4 SH `4167`, NJ-210-3.1 SH `4168`, NJ4-110-2.2 `4169`, NJ4-165-3.4 SH `4170`) — all started with no photo/features/specs/year/videos/tags. New `comau_recon.py` + `fix_comau_robots.py` (curated, id-keyed patch, `--apply --copy-media`). comau.com PDPs scrape over plain HTTP and the spec table parses as label-line→next-line, giving OEM-cited axes/payload/forearm-load/reach/repeatability/weight/IP/mounting for 6/6 (name convention NJ-210-3.1 → 210 kg/3.1 m agreed on all 6; fallback only). Heroes + galleries visually verified (download+Read) — rejected `_header` banner OG on 4168 in favour of the `NJ4-210_3.306` render, and rejected sibling cross-nav renders that each PRESS page embeds. Result: **6/6 updated, 0 created, 0 errors, copy-media 6/6, CDN-verify 54/54 HTTP 200 (company-wide)**; 3–6 photos, 8 TagCatalog tags (industrial-arm set, no Humanoid), 1 title-verified Comau video each; RobotInformationSource upserted 2–3/robot (OEM PDP + DirectIndustry / Flex-Line Automation 2022 brochure / RoboDK / SprutCAM). `enriched_at` **confirmed stamped 6/6 via prod DB** (`kubectl … Robot.objects.values('id','enriched_at')`) — note the research API does **not** serialize `enriched_at`, so `r.get('enriched_at')` is always None and must not be read as a blocker (supersedes the Ecovacs entry's BLOCKER note below). `payload_kg`/`reach_mm`/`repeatability_mm` are not Robot columns and silently drop — cited specs kept in features+notes. release_year/price left blank on purpose (no per-model launch citation; 2016 catalogue = existence, not launch). Zero published robots touched (400–405 skipped by status filter). Preview: `staging/reports/comau-fix-preview.json`; recon: `comau-recon.json`.
- Company 1461 (Jaten Robot) **audit + specs pass** (stakeholder flagged my earlier "complete" as wrong — `robot_gaps` only checks an image *string* exists, never that it loads, and ignores year/availability/specs). Real state found: 5× `SDM100/200/300/500/1000-335-MG0` shared one **404** image URL (146 B of HTML); every robot **<4 photos**; **no release_year on any**; availability missing on 5; only 2/14 had any typed spec. Actions: (1) `fix_jaten_specs.py --apply` → **14/14 updated** — `length_mm/width_mm/height_mm` on all (5 from the live OEM spec table, 9 parsed from their own stored OEM feature string), `speed`+`battery_capacity`+`voltage` on the 5 live models, availability on the 5 blanks; also **corrected R2SDM1500-335-MG0 dims** to the OEM's L1190×W860×H280. (2) `fix_jaten_phantoms.py --apply` → the 5 dead-photo models retired to **status=rejected** (backed up to `staging/reports/jaten-phantoms-backup.json`; Robot has no soft-delete flag, so `rejected` is the reversible removal mechanism — chose it over an irreversible hard delete). Evidence they're phantoms: zero hits in the live 267-card catalog, zero on CN `jtrobots.com` (different SKUs, e.g. SDM1000-D287-MGO), **zero exact-match web results**, and claimed chassis L1200×800×300 conflicts with the real 335-MG0 platform. Final: **9 pending_review (all photos verified HTTP-200 real product shots), 5 rejected**. Left open: `release_year` NULL on all (no citation on PDP/spec-PDF/press) and **≥4 photos unreachable** — Jaten publishes exactly 1 product image per model (Playwright-confirmed; other page images are shared chrome), the spec PDF adds 1 more real photo but it exists **only inside the PDF** and the API takes `images` as URL strings only (admin photo-upload is session-auth), so it can't be attached without a manual upload. (3) `fix_jaten_pdf_notes.py --apply` → **9/9**: the OEM Product Specification **PDF URLs are now recorded on the robots themselves** (they were only in this changelog before, i.e. invisible to whoever works the queue) — 5 live models get PDP+PDF in `information_sources` plus a `notes` line naming the PDF and stating that page 1 holds the higher-res photo an admin must upload manually (pages 3+ are line-drawings); the 4 delisted get a "not in the live catalog, no OEM PDP/datasheet — do not re-hunt" note (3 of them also note their hero is legitimately shared with platform sibling SDM500-D228). Two API traps found: bulk-import patch **silently skips `information_source_urls` when the robot already has any source** (`not patch_existing or not has_sources or replace_media` → all-False; and `replace_media` would delete them) and never overwrites non-blank `notes` — both must go through **DRF PATCH** (`information_source_urls` → `upsert_information_sources`, upsert-by-url/never-delete).
- Company 1461 (Jaten Robot): finished the queue — 9 remaining pending_review AGVs (the "ghost" SDM-*/MN-* siblings skipped on 2026-07-14) had only `no_tags`+`no_videos` (image+features+OEM url already present). `fix_jaten_ghosts.py --apply` inherited the vetted TagCatalog AGV set (AGV/AMR/Autonomous/Autonomous Mobile Robot/Industrial/Intralogistics/Logistics/Material Handling/Warehouse Automation/Wheeled) + the same 3 official Jaten AGV demo videos (`ZUCj8KkxOkU`/`xL6YiRfujbA`/`uIgpMO-F6CM`, titled) from their enriched siblings — no invented specs/heroes. Gap-fill via bulk-import `patch_existing` (id-keyed). Result **9/9 updated, 0 created, 0 errors**; re-probe **0 incomplete across all 14** (company queue complete). Short OEM features (108–139 chars) left as-is (adequate: nav/mode/dimension/load).
- Company 32 (Ecovacs Robotics): greenfield TEST batch. Backfilled **6 pending_review** DEEBOT robot vacuums (mini 2 `4720`, T80 OMNI BLACK `4717`, T50 MAX PRO OMNI BLACK `4718`, T50 OMNI Black `4719`, X11 OmniCyclone `4681`, X12 OmniCyclone `4676`) via new `fix_ecovacs_robots.py` (curated ROBOT_DATA, id-keyed patch, `--apply --copy-media`). Each gap-filled 0→6 verified photos (hero visually confirmed via download+Read), 0→clean features, suction/battery/airflow specs (OEM PDPs), release_year with citation (CES 2025/2026, IFA 2025 press), price for X11/X12 (US MSRP $1,099.99 / $1,499.99), TagCatalog-exact tags, and title-verified official/review YouTube. Sources = OEM + third-party tech press (Vacuum Wars, The Verge, PCMag, Android Headlines, PR Newswire). Result: **6/6 updated, 0 created, 0 errors, copy-media 6/6, CDN-verify 6/6 HTTP 200**; RobotInformationSource upserted (2–4 per robot). `enriched_at` **confirmed stamped 6/6 via prod DB** (06:04–06:07 UTC, `quality_checked_at` too). NOTE: this entry originally carried a **BLOCKER** claiming `enriched_at` never stamps and that a prod deploy + `backfill_enriched_at` was needed — that was a **FALSE ALARM** and has been retracted. Root cause of the misread: the research API does **not serialize** `enriched_at`/`quality_flags`, so `r.get('enriched_at')` is always None regardless of the real DB value (absent key ≠ null). Verify via prod DB only: `kubectl … Robot.objects.filter(id__in=[…]).values('id','enriched_at')`. Robot `1955` reads None simply because it predates the feature — precisely what `manage.py backfill_enriched_at` exists for. Sources upserted fine. Zero published robots touched (X5 OMNI 1937 / X9 PRO OMNI 1939 / T50 MAX PRO OMNI 1941 skipped by status filter). Preview: `staging/reports/ecovacs-fix-preview.json`.
- Company 32 (Ecovacs) batch 2: backfilled 6 more pending_review (T90 OMNI BLACK `4716`, X9 PRO OMNI BLACK `4715`, GOAT A3000 LiDAR PRO `4680`, WINBOT W3 OMNI `4677`, Commercial K1 VAC `4678`, Commercial M1 scrubber `4679`) — **12/43 done**. Closed URL (two stored `/global/...` URLs 404'd → real pages on `/us/shop/...`), photo (5–6 each), features, **purpose** (distinct curated statement, not a description copy), release_year (K1=2025 / M1=2023 from precise Interclean exhibitor release dates; GOAT/T90/WINBOT=2026; X9 PRO=2025), OEM-cited specs (GOAT 18.3kg/680×540×336/7500mAh/IPX6/0.7m/s/62dBA; K1 20kPa/558×420×592/46kg/4h/700m²/h), video, family-appropriate tags (mower ≠ vacuum ≠ window ≠ commercial). Result **6/6 updated, 0 created, 0 errors, copy-media 6/6, CDN 6/6**. Added **`audit_ecovacs_media.py`** (run before every `--apply`): fails closed on diagram/spec-sheet heroes and on duplicate images by URL **or sha256 content hash**, within a gallery and across the batch — all 12 Ecovacs robots audited clean (**71 images, all distinct, zero cross-model reuse**). Flagged: 2478/1939 carry junk auto-tags (Drone/Humanoid/Dance/Collaborative Robot); 47xx seeds carry `movement=mobile` which patch mode won't overwrite; possible dup pairs 4715/1939, 4716/2478, 4680/1951 (reported, not merged).
- Company 220 (Estun) video titles: **644/648 RobotVideos had blank titles** — root cause was `search_youtube_series()` in `fix_estun_robots.py` discarding the oEmbed titles it fetched and storing bare URL strings. Fixed the bug (now returns/keeps `{url,title,description}` dicts; existing videos enriched at the call site too) and backfilled all blanks via new `fix_estun_video_titles.py --apply` (oEmbed the 56 unique URLs once, patch each robot's videos through bulk-import `patch_existing` backfill branch — fills blank titles only, never overwrites/creates). Result: **262 robots patched, 644 titles, 0 created, 0 errors**; verified **648/648 videos now titled**. Also surfaced mis-assigned clips (e.g. a UBTech "Walker S" humanoid video on an Estun robot) now visible for review. The 15 EMR-* cobot records had wrong heroes (yellow industrial arm / CO-ARC welding cart / CN "5-Pro" infographic / blank) and irrelevant generic videos; they overlap the current S-series CoDroid line (S3-60…S20-180). Per stakeholder ("replace media only"), `fix_estun_emr_media.py` re-sourced media: videos via DRF PATCH `video_urls` (clean delete+recreate — the only path that removes wrong videos without admin session; bulk-import patch never deletes) to 2 real ESTUN cobot demos (CoDroid metal-processing `rsxDqqyvdKI` + automotive cobots `Jy0i8DLl6Cw`); images via bulk-import `patch_existing+replace_media` (force-overwrites `image` only, preserves text/specs) to payload-matched official ESTUN cobot renders (EMR-5→S5-90, EMR-10→S10-140, EMR-15/20/25→S20-180) + copy-media. **15/15 verified**: 2 titled cobot videos each, CDN-200 heroes with expected render hash. Left alone: the EMR-vs-S-series naming/duplication (data-integrity, pipeline domain).
- Company 1369 (ACY Automation Inc.): content-queue was complete except `no_videos` on 24/90 EOAT-component SKUs (quick changers, vacuum/suction cups, mounting clamps, frame connectors, mini/twin-rod/rotary cylinders). Added `fix_acy_robots.py` (video-only, id-keyed patch, no media copy) mapping each SKU family to official ACY YouTube channel (`UCQmdLbGsptezOi5s9Q8NOOQ`) demo shorts, all oEmbed-verified; `--apply` → 24/24 updated (0 created/0 errors), no_videos 24→0. Prod list endpoint 502s intermittently — script wraps the fetch in a 12× backoff retry.
- Content-queue Records filters **Enrichment complete** / **Enriched (stamped)** were client-only on the current page (60 newest) — looked empty for older enriched robots. Fixed: server-side `records_filter` on `api/robots/` before pagination (`robots/queue_filters.py`). Research bulk-import still stamps `enriched_at` correctly; backfill historical: `manage.py backfill_enriched_at`.
- Company 773 (Kawasaki Robotics): replaced 56/56 logo OG primary photos with OEM `/tachyon/` product heroes (`fix_kawasaki_robots.py --apply --copy-media`); CDN verify 56/56; Made In JP kept; CL series release_year=2023 (automatica premiere). Report: `staging/reports/kawasaki-773-overnight-report.md`.
- Company 220 (Estun) full-fleet enrich: `fix_estun_robots.py --apply` — features/tags/videos/availability for 264/264; payload/reach cited in features+notes from EN list cards + name conventions; no media wipe; years/prices left blank (uncited / unpublished).

## 2026-07-15

- Company 220 (Estun): addlink EN image CDN 502 blocked copy-media. Re-sourced reachable CN heroes from `www.estun.com/scara/` + family `/uploads/` (palletizing / iER / S-Pro). `fix_estun_alt_heroes.py --apply-import --copy-media` → 59/59 copy, CDN verify 264/264. Lesson: never stage owned CDN sibling URLs for recopy.
- Company 12 (Amazon Robotics) re-enrich: CDN AccessDenied on Atlas/Ernie/Robin/Sequoia/Vulcan + domain mismatch vs auth-walled amazonrobotics.com. Company website to aboutamazon.com; Robin remapped; new heroes (CDN 200 for Ernie/Robin/Sequoia/Vulcan); Atlas left blank (Titan Atlas still is Kiva). `fix_amazon_robots.py --only 2678 1599 1589 1596 1595 2677 --apply --copy-media`.
- Enrichment evidence: extended `evidence_store.py` for lean page text + Gemini request/response extracts, `robot_id` on enrich manifest entries, HTML retention for `target_not_found` / low grounded scores, and `sweep_company_evidence` (keep last N runs / size cap). Wired into `robot_auto_research.research_robot`, overnight, and targeted enrich summaries (`evidence_dir` / `evidence_run_id`). Snapshot verify path in `verification.py` / `verify_lib.py` labels `verified_against=evidence_snapshot` and does not treat snapshot text as a live fetch for URL bands. Discovery wiring unchanged. See [concepts/evidence-and-target-not-found.md](concepts/evidence-and-target-not-found.md).
- Pipeline quality: **evidence storage + replay** — `evidence_store.py` saves raw HTML/text and Gemini extract JSON under `staging/evidence/{slug}/{run_id}/` during `discover_robots.py`; batches are re-validatable without re-fetch. **`target_not_found`** — discovery and enrich fail closed when a named robot/model is not confirmed on fetched pages (`confirm_target_on_page`); `select_images_for_pages` no longer falls back to sibling/whole-page images. Adapter registry **deferred** until the next ESTUN-class OEM — see [decisions/adapter-registry-deferred.md](decisions/adapter-registry-deferred.md). Concept: [concepts/evidence-and-target-not-found.md](concepts/evidence-and-target-not-found.md).
- Content-queue enrichment filter: added `Robot.enriched_at` (migration `0081`); stamped on research bulk-import rows (`[AI Research]` notes / `research_notes` / sources). Queue Records filter options: **Enrichment complete** (heuristic image+features+video+tags) and **Enriched (stamped)** (`enriched_at` set). Backfill historical rows: `python manage.py backfill_enriched_at` (optional `--only-pending` / `--dry-run`).
- Content-queue toolbar: Publishers + Issues are searchable multi-selects; issue filter ORs selected flags and uses live gap fallbacks (No photo / No features / No video / No tags / No URL) when `quality_flags` were never audited.

## 2026-07-14

- Overnight top-10 remaining: started `scripts/research/run_top10_overnight.py` → gap-only `overnight_queue_enrich.py --company-ids` for Estun(220), FANUC(189), EFORT(1479), MUKA(1480), BEWIS(1225), Wesar(1476), Pangolin(1413), Stäubli(1475), Yamaha×2(1484/1477). Patched blank OEM websites before run. Progress: `staging/reports/overnight-queue-progress.json`; log: `staging/reports/top10-overnight-run.log`. Mode is auto gap-fill (no Gemini / no curated visual QA).
- Company 1473 (RealMan Robotics): added `scripts/research/fix_realman_1473_robots.py` — 11/11 Standard/Force/Vision arms (ECO62/65, RM65/75, RML63, RX75); OEM Unicode `/prop/` heroes; website → `https://www.realman-robotics.com/`; copy-media 11/11; CDN verify 11/11; marked done 1473.
- Company 307 (Trossen) release-year retry: added `scripts/research/retry_release_years.py` — generic re-run of the grounded year lookup for pending_review robots with blank years (`lookup_release_year` fail-opens, so a rate-limit blip nulls a whole batch: 7/9 null in the morning enrichment run, 6/8 hits on same-day retry). Applied 3 vetted years (5266/5267→2024, 5273→2025; citations in notes); rejected datasheet issue-dates, dead YouTube listings, family-membership and partnership-announcement evidence. Playbook section added to `.claude/commands/research-robots.md`. Also confirmed: enrichment path (`robot_auto_research.py`) has NO price extraction — only discovery (`discover_robots.py`) does — so Trossen queue prices stayed blank despite plain-text OEM prices on trossenrobotics.com; port pending.
- Company 1512 (Jiangsu DINGS): added `scripts/research/fix_dings_robots.py` — Gripper series (5209) from USA site + ENG catalog; copy-media 1/1; marked done 1512.
- Company 1511 (Auris Health / J&J MedTech): added `scripts/research/fix_auris_robots.py` — MONARCH QUEST (5197) image/features/tags/videos from jnjmedtech PDP + 510(k) press; copy-media 1/1; marked done 1511.
- Company 1461 (Jaten Robot): added `scripts/research/fix_jaten_robots.py` — enriched **5/14** live-catalog AGVs (list-card URL remap, OEM/Red Dot heroes, features/tags/videos); skipped 9 delisted/wrong-ID CRM rows; company website → `https://jaten-robotics.com/`; copy-media 5/5. Not marked done.
- Company 882 CDN repair: all 15 Realman heroes were owned CDN path strings with empty `s3_image` (CloudFront AccessDenied). Re-sourced OEM `/prop/...` heroes, `fix_realman_robots.py --apply --copy-media --verify-cdn` → 15/15 HTTP 200. Added `scripts/research/verify_cdn_images.py`. Hardened `robots/background.py` copy worker to fail closed when image is owned CDN without s3_image.
- Company 307 (Trossen Robotics): added `scripts/research/fix_trossen_robots.py` — 9/9 pending enriched (Wix + docs X-series heroes; product-demo YouTube; dof typed; payload/reach in features). Rejected TOTL PC / tutorial / HG graphics. copy-media 9/9; skipped published 5265; marked done 307.
- Company 882 (Realman Robotics): added `scripts/research/fix_realman_robots.py` — filled videos for ECO62/63/RM75/RX71/75, replaced ECO65 mistagged clip, typed OEM payload/reach/dof for 15/15; CDN heroes kept; marked done 882.

## 2026-07-13

- Company 1375 (AE Robotics): added `scripts/research/fix_ae_robots.py` — enriched **19** AE-own/white-label robots via automationar.com (aerobot.cc dead); rejected AUBO/TYYROBOT junk heroes; copy-media 19/19. Left 22 AUBO/Fanuc/JAKA/UR/Youibot/Trans reseller gaps; company not marked done.
- Company 1206 image QA: confirmed DRS60×6 share one OEM hero (no per-SKU still); DRS30/40C4/40L3 are different files but same SCARA look; upgraded DRS40L3 pair to SS2 hero; DRV90L7R65 → distinct high-res still vs DRV70 twin pose.
- Company 1206 (DELTA Electronics Shanghai): video QA — removed private `hpkLdzvmjWY`, DRAStudio `7n3aldac1EM`, and Power Quality `hcPciVWZYpI`; replaced with title-verified SCARA/DRV demos; `youtube_metadata.enrich_video_list` now skips empty/rejected titles; Video quality gates added to content-queue skill.
- Company 1206 (DELTA Electronics Shanghai): added `scripts/research/fix_delta_robots.py` — 17/17 pending enriched via `landing.deltaww.com` heroes + 2022 robot catalog specs; replaced about/banner junk + boilerplate features; copy-media 17/17; marked done 1206.
- Company 50 (IIT): extended `fix_iit_robots.py` for HyQ2Max (5206) + MiniHyQ (5207) — fixed 404 URLs, DLS carousel heroes, features/specs/videos/tags; `--names` filter; copy-media 2/2; `no_image` cleared. Company not marked done (other soft gaps remain).

## 2026-07-12

- Company 12 (Amazon Robotics): added `scripts/research/fix_amazon_robots.py` — fixed shared Kiva DU 1000/3000 heroes (distinct orange vs Hercules blue), Sparrow missing image, Scooter official photo, Wikipedia/CJK/blog junk features on 10 pending robots; copy-media 5/5. Atlas photo not replaced (no verified tote-cart still). Marked done 12.
- Company 237 (Segway Robotics): added `scripts/research/fix_segway_robots.py` — gap-only patch for 5/5 pending (videos+tags+movement; Nova Carter typed weight/dims/battery/runtime from OEM milestones). No image overwrite, no Gemini, no copy-media. Soft leftover: payload packed into notes is skipped when notes already filled; RMP/DeliveryBot lack OEM numeric tables. Marked done 237.
- Overnight queue drain **completed** (`overnight_queue_enrich.py`, ~14.8h, Gemini off): 256 companies in queue → 160 attempted + 96 soft-skipped (no website, 728 gapped robots untouched). Researched 1322 / imported 1321 gap-only patches; copy-media 227; marked done 30 companies; 0 failures. Summary: `staging/reports/overnight-queue-summary.json`. Remaining gaps common where auto-scrape leaves features &lt;40 chars (Estun/Yaskawa/ACY/KUKA catalogs).

## 2026-07-11

- Overnight queue drain: added `scripts/research/overnight_queue_enrich.py` — walks `pending_review` oldest→newest by `created_at`, gap-fill only via `_merge_staged` + bulk-import `patch_existing` (no `force_overwrite`, no Gemini/`grounded`). Progress: `staging/reports/overnight-queue-progress.json`. Soft-skips companies without a website.
- Queue triage: added `scripts/research/triage_content_queue.py` (gap score → company rank; state in `state/content_queue_done.json`; report `staging/reports/content-queue-triage.json`). Seeded done: 50, 52, 342, 783, 975, 1073, 1369, 1396, 1445, 1398, 1424. Marked done: 1028 (Noblelift), 810 (Mujin), 49 (Hyundai Robotics).
- Company 49 (Hyundai Robotics): added `scripts/research/fix_hyundai_robots.py` — 23/23 pending enriched via `www.hd-hyundairobotics.com/api/v1/product/page` (size≤10, unwrap envelope) + HDC static thumbs; URL remap to `hd-hyundairobotics.com`; English curated series/app copy; copy-media 23/23; QA bad_count=0; `verify_content` deferred.
- Company 810 (Mujin): added `scripts/research/fix_mujin_robots.py` — 11/11 pending enriched (solution URL remaps, holders/uploads heroes, English curated copy, tags/videos); copy-media 11/11; `verify_content` deferred (QA/English first).
- Company 1028 (Noblelift): added `scripts/research/fix_noblelift_robots.py` — 55/55 pending enriched (ASPX Ispic heroes, URL map + overrides for bad itemids, table/subtitle specs, tags/videos); copy-media 55/55; 0 incomplete remaining.
- Company 1424 (SIASUN): added `scripts/research/fix_siasun_robots.py` — 39/39 pending enriched via EN PDP scrape (heroes/features/payload-reach/tags/videos); copy-media 39/39; SA7A/SA10A remapped CN→EN.
- Company 1398 (Geek+): added `scripts/research/fix_geekplus_robots.py` — 32/32 pending enriched (official HubSpot heroes, technology pages, RS/F12ML specs, tags/videos); copy-media 32/32; 0 incomplete remaining.
- Company 1445 (Epson Robots): added `scripts/research/fix_epson_robots.py` — 25/25 pending enriched (PDP URLs, adaptivemedia heroes, features/specs/tags/videos); copy-media OK; 0 incomplete remaining.
- Company 975 (EVE Energy): added `scripts/research/fix_eve_robots.py` — 50E photo + videos/tags for 50E/50PL/LF105; copy-media 3/3. Official EVEMALL images need Referer.
- Company 783 (Infinium Robotics): added `scripts/research/fix_infinium_robots.py` — Infinium Scan photo/features/cited specs/3 videos/tags; copy-media OK. Catalog discovery: only one named robot on OEM site (`/packages` = pricing).
- Company 50 (IIT): added `scripts/research/fix_iit_robots.py` — 7 pending robots enriched (GrowBot, HyQReal, XoSoft, PLANTOID, I-Seed, HyQCentaur, HyQ); IIT lab URLs; corrected heroes; features/specs/videos/tags; copy-media 7/7. Skipped published iCub/ErgoCub.
- Added project skill `.cursor/skills/content-queue-robot-backfill/` (SKILL.md + self-learning `lessons.md`) for reusable content-queue robot backfills; linked from project-onboarding + robot-research-agent.

## 2026-07-29

- Company 305 (Avidbots): `fix_avidbots_305_robots.py` — enriched Meet Kas #3847, Neo 2 #3845, Neo 2W #2679; rejected Neo 2W dupe #3846; cleared fake payloads; CA country; CDN verify 6/6. Report: `staging/reports/avidbots-305-enrichment.md`.

## 2026-08-01

- Company 1602 discovery follow-up: `fix_rsm_1602_gaps.py` — photos for SC7-1077 #6806, SC15-1464 #6808, SC20-2027 #6809 (distinct OEM shots; rejected mounting diagram / welder panel); Robotic Laser Welding #6810 got dof=6 + sensors/connectivity/voltage from OEM laser table (arm payload/reach still unpublished). CDN 4/4 OK.
- Company 1602 discovery: `discover_rsm_1602_robots.py` — created SC7-1077 #6806, SC6-1460 #6807, SC15-1464 #6808, SC20-2027 #6809 (column-correct cobot table), Robotic Laser Welding #6810; CDN OK for imaged keepers. Company now has bending cell + SC family + laser welding cell.
- Company 1602 (RSM Machinery / ERSM): `fix_rsm_1602_robots.py` — enriched #6718 Robotic Bending Cell (6-DOF, 20 kg, 1780 mm, ±0.03 mm; CN; taxonomy/tags; OEM cell hero); rejected 18 non-robot machine-tool/ASRS rows as `wrong_category`. CDN keeper OK.
- Company 1605 (Shandong Tengyang Intelligent Equipment / Kowell): `fix_tengyang_1605_robots.py` — renamed 6 pending robots from marketing titles to OEM model codes (KW1300M-3200, KW1120M-2400, KW1030M-1835, KW1080B-2700, KW1007B-740, KW1004B-580); families KW-M / KW-B; filled features, purpose, typed specs, CN country, taxonomy, tags; CDN verify 6/6; KW1007B payload=7 kg (table 4 kg noted as sibling contamination). Soft left: few_photos, price, video, release_year.

## 2026-07-10

- Company 1073 (INTAMSYS): added `scripts/research/fix_intamsys_robots.py` — replaced wrong/shared photos with official FUNMAT product renders; backfilled features, weight/dimensions from comparison table, 2 YouTube videos each, curated manufacturing tags; copy-media 5/5 OK (NEO, HT, PRO 410, PRO 610HT, PRO 310 APOLLO).
- Company 52 (Intuitive Surgical): added `scripts/research/fix_intuitive_robots.py` — curated enrichment for 4 pending robots (Da Vinci SP/5, Ion, Instruments) after live intuitive.com Incapsula blocked scrapers; official CDN images + Wayback/Serper copy + YouTube videos; dof=7; copy-media 4/4 OK. Skipped published Da Vinci Xi (60).
- Company 1369 (ACY Automation Inc.): backfilled country TW, product_stage, focus, sources/description from acy.com.tw; logo uploaded (`ACY-Logo-4-1.jpg`). Fixed `patch_company_logo` client (drop session JSON Content-Type) and `get_country_id` exact-code lookup. Staging: `staging/companies/acy-automation-inc.json`.
- Company 1396 (KUKA): company backfill applied (website, description, HQ Augsburg/DE, leadership, employees ~14.5k, status/focus). Logo upload still blocked by API multipart/JSON error — upload manually in admin if needed. Staging: `staging/companies/kuka.json`.
- Company 1396 (KUKA): added `scripts/research/fix_kuka_robots.py` — family-page catalog scrape (payload/reach/images/videos), name-code spec fallback, tags via catalog suggest; 81/81 pending robots updated.
- Company 342 (iRobot): added `scripts/research/fix_irobot_robots.py` — PDP scrape for photos/features/specs/videos; import one robot at a time (slugify collisions on `j9`/`j7`/`i3+`); shop-anchor URLs mapped to real SKUs; YouTube search fallback when PDP has no embeds.
- Added [playbooks/catalog-oem-import.md](playbooks/catalog-oem-import.md) — step-by-step catalog OEM import (Estun template).
- Added [reference/estun-import-postmortem.md](reference/estun-import-postmortem.md) — what worked/failed on Estun prod import.
- Added [concepts/catalog-vs-enrich-pipeline.md](concepts/catalog-vs-enrich-pipeline.md) — catalog parser vs `auto pipeline` decision guide.
- Updated [index.md](index.md) — navigation and document hierarchy.

## 2026-07-09

- Added [checklists/prod-manufacturer-import.md](checklists/prod-manufacturer-import.md) — prod import guardrails after Estun Robotics lessons (catalog path, S3 recopy, no duplicate enrich).
- Added [index.md](index.md) — bundle index for research documentation.
- Updated prod import checklist: Estun images redirect to `img.addlink.cn`; server must allow Estun OEM host when copying to S3.
- Noblelift (1028) source-extension test batch: `fix_noblelift_sources.py` patched 6 most-gapped pending_review robots (RT20G, RT16/20Pro-RT16/20B, RRS-150/180, RT16Li, RT15Q, NR530) — dims/speed/voltage/battery/weight from column-aware OEM spec-table parse, availability=available, sources 1→3-4 (OEM PDP + nobleliftgroup.com + official regional subsidiaries), enriched_at 6/6, CDN 55/55. `noblelift.com` confirmed as the OEM product site (nobleliftgroup.com is the corporate site, category pages only). release_year left blank — no credible citation. OPL10 (3373) excluded: its PDP documents OPL12N/OPL25N only, no OPL10 exists on the OEM site — flagged for human review.
- Noblelift (1028) follow-up: `fix_noblelift_descriptions.py` hand-wrote 55 Track A descriptions (short_description 55→0; applied via surgical PATCH since patch-mode won't overwrite non-blank text and force_overwrite wipes features/media); `fix_noblelift_sources.py --all` took missing_availability 46→2 and enriched_at to 53/55. missing_specs 30→29 is the honest floor: AGV PDPs publish specs as images (zero <td> rows) and the PT*/PS*C-D-R rows' CRM URLs document other models. Fixed a real unit-cell shift bug (full-width `Q（ kg ）` shifted FEP values one column → FEP-30P would have taken 2500kg not 3000kg) and added family-variant expansion to the coverage gate.
- Ecovacs (32) queue finish: `fix_ecovacs_thin_content.py` patched **28/31** remaining pending_review (direct DRF PATCH — bulk-import patch mode is a no-op against non-blank junk, force_overwrite wipes media). Removed **ULTRAMARINE P1 pool-cleaner boilerplate** that was pasted verbatim as `features` on 13 records incl. the AIRBOT Z1 air purifier (now 0/31); junk tags (Drone/Humanoid/`long`/`round`/`space`) 31→0; purpose curated 28/28; 24/31 now carry ≥1 typed spec; release_year cited on 13, NULL on 15 (no per-model citation); every `url` moved from a category page to a real PDP (catalog enumerated from `global/pdp_sitemap.xml`, 79 PDPs; `/us/shop/` PDPs resolve but are in no sitemap). **3 left for a human**: 2479/2480 ("DEEBOT X11/X12 OMNI" — no such SKU, probable dups of 4681/4676) and 1952 ("GOAT A2000" — no OEM PDP, only A2000 LiDAR PRO); tags-only fix applied to those since junk tags are wrong regardless of SKU. Caught a self-inflicted fabrication pre-apply: "81mm" carried onto the X5 family from T50 (X5 PDPs state no figure). **OPEN — company-wide media defect**: `audit_ecovacs_media.py --live` (new mode) proves galleries are byte-identical across families — one render is the hero of 12 vacuums, a 2560x600 banner is the hero of 11 records, a two-people photo sits on 14, 2517's hero is a dead 503. Also fixed the audit's own Content-Type guard → magic bytes (our CDN serves octet-stream, so it was reporting a false clean).
- Ecovacs (32) **media repair — the open defect above is now CLOSED**: `detect_duplicate_heroes.py --company-id 32` exits **0** (46 robots, 38 heroes, **38 distinct**, 0 errors; was 21 distinct / 4 shared clusters covering 29 robots). `fix_ecovacs_media.py` re-sourced **23** pending_review robots to per-model PDP media — **129 images, all byte-distinct company-wide**, 0 copy-media drops, every CDN object HTTP 200 + valid magic bytes + extension matching sniffed format. Media-only: went **around** `import_staging()` and called `bulk_import_robots(patch_existing=True, replace_media=True)` directly, because the client validator forces `sources` into the row and `sources`+`replace_media` **deletes the curated RobotInformationSource rows** (bulk_import_media.py:200-213); verified on 1937 that features/description/tags/videos/sources were untouched. **8 fail-closed** (junk media cleared via `kubectl exec … -c django-app` — `images:[]` is a no-op and `image=''` alone still resolves via the primary photo's s3_image; admin delete-photo 403s on X-Internal-Secret — each carries an actionable `[IMAGE TO-DO]` note): **1939/1949/2473/2479/2480 = duplicate records** of 4715/4677/4720/4681/4676 (proved by hashing candidates against out-of-batch robots; X9 PRO OMNI is a black-only SKU); **1952 GOAT A2000** and **1964 GOAT G1 Plus** = SKUs that don't exist (G1 line is G1/G1-800/G1-2000); **2475 N30 PLUS** = its PDP has N30 PLUS copy but all 25 images are `DEEBOTN20ePlus_*`. **AIRBOT Z1 (1965) SAVED from fail-closed**: PDP is gone from every region but its `site-static.ecovacs.com` objects are still live — recovered the original URLs from a Wayback snapshot (`/global/airbot-air-purifier-robot/airbot-z1`, note `-robot`) and fetched from ECOVACS; 5 genuine renders. Bugs found in our own tooling: **`920x920` contains the substring `X9`** (dimension tokens must be stripped before model-token matching — it silently dropped every correct T90/GOAT-G1 asset), substring matching admits sibling SKUs (W2S vs W2S OMNI), and `/es/shop/<any-slug>` is a **soft-404** serving the brand homepage at HTTP 200. **copy-media dropped nothing, but this did NOT test the WebP fix** — Ecovacs serves JPEG/PNG only (102 jpeg + 27 png, correct `image/*` headers).

- 2026-07-25 Company 1381 (Agile Robots): 8 enriched (family+movement fix+specs+de-contaminated features+narrative fields), 1559 rejected (AMR/AGV shell); fixed RobotSerializer narrative-field gate. Script: fix_agile_robots_1381.py

- 2026-07-26 Company 1375 (AE Robotics): queue was a **reseller catalog** — the `Brand:` field on every automationar.com PDP showed only 6 of 23 pending robots were AE's own. 9 rejected as `duplicate`+`wrong_company` (each duplicating a better OEM-side record), 8 moved to FANUC/AUBO/YOUIBOT and enriched there, 4 AE/A&R-own enriched. Fixed real spec errors: FANUC R-2000iC/125L payload 25→125 kg, welding station `payload_kg` 350 → `weight_kg` 350, Trans 200/500 payload 3.0 → 200/500 (from the official DCM200/DCM500/FCP150 brochures, which are the only surviving source for these discontinued SKUs). Removed 31 rights-restricted/wrong-model photos + 15 wrong-product videos; 6 robots fail-closed with `[IMAGE TO-DO]` notes. Gotchas: `company_ref` must be written together with `company_owner_ids` or it snaps back; bulk-import `company` read pre-patch created a duplicate robot (5616, deleted); AUBO's `iF` series is force-control, NOT the explosion-proof FB line; AUBO company website was the dead aubo-robotics.com → aubo-cobot.com. Scripts: `fix_ae_reseller_spillover.py`, `fix_ae_own_brand_robots.py`
